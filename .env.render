# =====================================================
# CONFIGURAÇÃO PARA PRODUÇÃO NO RENDER.COM
# =====================================================

# Redis do Render (usar o Internal URL fornecido)
REDIS_URL=redis://red-d1c4jheuk2gs73absk10:6379

# Configuração alternativa se precisar de senha
# REDIS_URL=redis://:password@red-d1c4jheuk2gs73absk10:6379/0

# Database PostgreSQL (já configurado automaticamente pelo Render)
# DATABASE_URL é fornecido automaticamente

# Credenciais do Portal Atacadão (IMPORTANTE: Configurar no Render Dashboard)
# ATACADAO_USUARIO=seu_usuario_producao
# ATACADAO_SENHA=sua_senha_producao

# Configurações de Worker
RQ_WORKER_TTL=300  # 5 minutos
RQ_RESULT_TTL=86400  # 24 horas
RQ_DEFAULT_TIMEOUT=1800  # 30 minutos

# Flask em produção
FLASK_ENV=production
FLASK_DEBUG=0

# Segurança
SECRET_KEY=gerar-chave-segura-aleatoria-aqui