# 🚀 SISTEMA DE FRETES - REQUIREMENTS PRINCIPAL
# Versão com NLP integrado para melhor entendimento do usuário

# ==========================================
# CORE FRAMEWORK
# ==========================================
Flask==3.1.2
asgiref>=3.7.0  # Flask async views (flask[async] extra)
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-Session==0.8.0
Flask-WTF==1.2.2
WTForms==3.2.1

# ==========================================
# DATABASE & DATA PROCESSING
# ==========================================
psycopg2-binary==2.9.11
SQLAlchemy==2.0.46             # patch (transitiva via Flask-SQLAlchemy)
Werkzeug==3.1.5                # patch (transitiva via Flask)
certifi>=2026.1.4              # CA roots atualizados (transitiva via requests)
pandas==3.0.0
openpyxl==3.1.5
# xlsxwriter removido - usando openpyxl com template
xlrd==2.0.2
xlwt==1.3.0
python-dateutil==2.9.0.post0
pytz==2025.2
requests==2.32.5
urllib3>=2.6.0             # CVE-2025-66418 (decompression bomb)

# ==========================================
# CLOUD & DEPLOYMENT
# ==========================================
gunicorn==25.1.0  # CVE-2024-6827 fix (HTTP smuggling)
greenlet==3.3.1  # CRÍTICO: Versão 3.1+ para Python 3.12+
gevent==25.9.1  # Atualizado para compatibilidade com greenlet
boto3==1.42.50
python-dotenv==1.2.1
psutil==7.2.2

# ==========================================
# CACHE & PERFORMANCE
# ==========================================
redis==7.2.0                # 5.0.8 → 7.2.0 (API core compatível, requer Python 3.10+)
rq==2.6.1                   # 1.16.2 → 2.6.1 (Connection ctx removido, worker_ttl renomeado)
# rq-dashboard removido — abandonware (2020), não importado no app, incompatível com rq 2.x
APScheduler==3.11.2
cachetools==5.5.2          # Bounded in-memory caches (TTLCache) — OOM prevention
whitenoise==6.11.0  # Serve arquivos estaticos com compressao e cache no WSGI layer
Flask-Compress==1.23  # Compressao gzip de respostas dinamicas (HTML, JSON)

# ==========================================
# VALIDATION
# ==========================================
dnspython==2.8.0
email-validator==2.3.0
zxcvbn>=4.4.28              # Password strength estimation (seguranca module)

# ==========================================
# AI & CLAUDE INTEGRATION
# ==========================================
anthropic==0.84.0           # API direta Anthropic (Structured Outputs, top-level cache_control, MCP converters)
claude-agent-sdk==0.1.48    # Claude Agent SDK oficial (0.1.48: CLI 2.1.71, fix input_json_delta streaming #644)
mcp>=1.26.0                 # Model Context Protocol (latest stable)
pydantic==2.12.5           # Data validation
anyio==4.12.1              # Async support

# ==========================================
# NLP PARA MELHOR ENTENDIMENTO (NOVO!)
# ==========================================
# Essas bibliotecas melhoram DRASTICAMENTE o entendimento:
# - Correção ortográfica automática
# - Fuzzy matching (encontra "Assaí" quando usuário digita "asai")
# - Análise sintática e semântica
# - Detecção de sinônimos

fuzzywuzzy==0.18.0         # Matching fuzzy de strings
rapidfuzz==3.14.3          # Substitui python-Levenshtein - mais rápido e compatível com Python 3.12+
# python-Levenshtein-wheels==0.13.2  # Alternativa se precisar manter Levenshtein
unidecode==1.4.0           # Remove acentos para comparação

# NLTK Simplificado (apenas o essencial)
nltk==3.9.2                # Natural Language Toolkit

# Fuzzy matching adicional (necessário para entity_mapper)
jellyfish==1.2.1           # String matching algorithms

# SpaCy - Processamento Avançado de Linguagem Natural
# spacy==3.7.4               # Engine principal de NLP
# IMPORTANTE: Após instalar, execute:
# python -m spacy download pt_core_news_sm
# ou adicione no seu script de deploy:
# import spacy; spacy.cli.download("pt_core_news_sm")

# ==========================================
# MACHINE LEARNING (OPCIONAL)
# ==========================================
# scikit-learn==1.4.0      # Para predições e clustering
# numpy==1.26.4           # Computação científica

# ==========================================
# VISUALIZAÇÃO (OPCIONAL)
# ==========================================
# plotly==5.20.0          # Gráficos interativos
# matplotlib==3.8.3       # Gráficos básicos
structlog==25.5.0  # Versão específica para estabilidade
# Logging colorido
colorlog==6.10.1  # Versão específica para estabilidade
PyJWT==2.11.0

fastapi==0.129.0
uvicorn[standard]==0.41.0
python-multipart==0.0.22
python-jose[cryptography]==3.5.0
httpx==0.28.1
# aioredis removido — deprecado (merged em redis-py 4.2+), não importado no app
# python-json-logger removido — não importado em nenhum arquivo do app (structlog+colorlog são usados)
xlsxwriter==3.2.9

# ==========================================
# EMAIL PROCESSING
# ==========================================
extract-msg==0.55.0  # Para ler arquivos .msg e .eml do Outlook

# ==========================================
# PORTAL INTEGRATION - BROWSER AUTOMATION
# ==========================================
# Escolha uma das opções abaixo conforme necessidade:

# Opção 1: Selenium (para Chrome Debug Mode - RECOMENDADO)
selenium==4.40.0           # Automação browser (atualizado)
webdriver-manager==4.0.2   # Gerenciamento de drivers (atualizado)

# Opção 2: Playwright (alternativa moderna)
playwright==1.58.0         # Automação multi-browser (compatível com greenlet 3.3+)
nest-asyncio==1.6.0        # Fix para Playwright Sync em loop asyncio

# Dependências adicionais para portal
cryptography==46.0.5       # Criptografia de cookies/sessões (OpenSSL patches)
# PyPDF2==3.0.1            # Removido - pypdf é o sucessor
pdfplumber==0.11.9
pypdf==6.7.1

# ==========================================
# RASTREAMENTO GPS E QRCODE
# ==========================================
qrcode==8.2              # Geração de QR Codes
pillow==12.1.1           # Processamento de imagens (CVE-2026-25990 fix)
haversine==2.9.0         # Distância entre coordenadas GPS
# Geocoding agora usa Google Maps API (mesma do mapa_service.py)
weasyprint==68.1         # Geração de PDFs
python-barcode>=0.15.1   # Code128 barcode para DACTE (pure Python, SVG)

# ==========================================
# OCR / PDF PROCESSING (Comprovantes)
# ==========================================
pypdfium2==5.4.0           # Renderização de PDF para imagem (leitor comprovantes)
tesserocr==2.10.0          # OCR via Tesseract C API (requer tesseract-ocr no sistema)

# ==========================================
# EMBEDDINGS & VECTOR SEARCH
# ==========================================
voyageai>=0.3.0            # SDK Voyage AI (embeddings + reranking) — recomendado pela Anthropic
pgvector>=0.3.0            # Python bindings para pgvector (busca vetorial no PostgreSQL)

# ==========================================
# APPLICATION MONITORING (APM)
# ==========================================
sentry-sdk[flask]==2.54.0  # Error tracking, performance monitoring, distributed tracing