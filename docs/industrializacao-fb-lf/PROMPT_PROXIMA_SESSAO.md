<!-- doc:meta
tipo: scratch
camada: L3
sot_de: —
hub: docs/industrializacao-fb-lf/INDEX.md
superseded_by: —
atualizado: 2026-06-13
-->
# PROMPT — Próxima sessão (Industrialização FB↔LF)

> **🎯 DESTINO (NORTE FIXO, validado Rafael 15/06):** industrialização FB↔LF **100% automática e fechada** — 8 critérios de pronto em `SOT_OPERACOES.md §"OBJETIVO FINAL"` (R1 emissão auto · R2 escrituração auto · R3 vínculo · estoque+26489 · contabilização pelo ciclo · durável · L0 · piloto SEFAZ). **Reportar progresso contra esses 8 critérios a cada sessão.** Rafael = dono do destino; condução dos meios = Claude Code.
>
> **✅ 2026-06-15 (CANARY G1 — estágios 0+1+2+3 OK, draft criado/postado/revertido, zero rabo) — montagem server-side + baixa PASSIVA provadas SEM SEFAZ.** Estágios 0✅ (oráculo) 1✅ (genealogia `safe_eval`==oráculo) **2✅** (NF-2 **798563** draft em RETIND 1083: 16 linhas · CFOP 5902 · CST 50 · conta 1150100012 · R3 · total **90,87** · `validar` ok) **3✅** (post draft→posted `cstat=vazio` · **baixa PASSIVA 5101020001 = R$ 90,87 pelo ciclo** · revertido `button_draft`→`unlink`). `s71` agora tem `--montagem`/`--postar`/`--reverter` (derivam o `SA_BODY_G1`, dry-run default, `--confirmar` gated). **Achado:** `validar`/`medir` comparam total **por-linha** (`_total_esperado_por_linha`, Σ `round(qty·price,2)`=90,87) e não o float (90,89) — sem afrouxar tolerância. Suíte serviços **33 verde** (+3). Commits `ee9372eed`/`91f9fed6e`/`58aa1acaf` + **a commitar** (fix validar/medir + s71 estágios 2-3 + docs) na branch `feat/wire-r2-industrializacao` (**NÃO pushada**). **🔴 RETOMAR NO ESTÁGIO 4** (provisionar SA+cron + G2 SEFAZ piloto, **go duplo**): `app/odoo/estoque/fluxos/1.1.4 §"CANARY — RUNBOOK"` (0✅ 1✅ 2✅ 3✅ 4⏭️). Detalhe `ACHADOS §"CANARY G1 — estágios 2+3"`.
>
> **✅ 2026-06-15 (FOCO 1 — SA DURÁVEL DA SAÍDA G1+G2) — ARQUITETURA TRAVADA (Opção B) + FRAMEWORK dry-run-first.** Worktree `feat/wire-r2-industrializacao` (NÃO pushado/executado, ZERO escrita Odoo). DECISÃO `SOT §6.3`: `ir.cron`-Odoo + SA persistente server-side; genealogia server-side; `DescobertaIndustrializacaoService`=spec+oráculo; Q2 resolvido (`PROTECAO §N34`: montagem da saída no body da SA, não átomo — *lying parameter*). Construído (28 pytest, suíte 868→**896**): `orchestrators/saida_retorno_industrializacao.py` (READ planejar/validar/medir) · `provisioning/sa_retorno_industrializacao.py` (provisionador idempotente + `verificar`; bodies G1 montagem [🔴 genealogia safe_eval PENDENTE CANARY] / G2 fiel `s54`) · `provisioning/monitor_sa_industrializacao.py` + hook `SessionStart`. **🔴 PRÓXIMA SESSÃO:** (1) **CANARY READ-only body G1** (resolver chave-remessa-RPI do picking `# 🔴 CANARY` + validar NF-2 vs oráculo `saida_retorno_industrializacao validar`, go fresco p/ provisionar SA) · (2) confirmar campos `ir.cron` no Odoo vivo + ligar cron G1 · (3) G2/SEFAZ piloto (go fresco) · (4) reconciliar 26489 · (5) FOCO 3 constants L0. Folha `fluxos/1.1.4`. Detalhe: `ACHADOS §"SESSÃO 2026-06-15 (FOCO 1..."` + `SOT §6.3`.
>
> ⬇️ **Banner anterior (WIRE da ENTRADA — já concluído):**
> **✅ 2026-06-15 (tarde) — WIRE do R2 (FOCO 1) CONSTRUÍDO dry-run-first.** Worktree `feat/wire-r2-industrializacao` (NÃO commitado — aguarda go). Orchestrator C3 `app/odoo/estoque/orchestrators/entrada_retorno_industrializacao.py` compõe o FLUXO L3 1.2.4 end-to-end (8 modos CLI, cada escrita gated `--confirmar`) + **3 átomos R3 NOVOS na Skill 7** (`resolver_chave_remessa`, `marcar_vinculo_r3`, `postar_invoice`). 25 pytest novos · suíte `tests/odoo` 868 verde · **validado DRY-RUN contra PROD** (plan resolve chave RPI/DFe 44523/total rateio 90,89; escriturar-nf1-po idempotente→PO 43464; medir→ATIVA=0). Detalhe: `.remember/remember.md` + ROADMAP_SKILLS + fluxo 1.2.4. **Pendente**: commit + execução real (go) + D reconciliar 26489 + FOCO 2 (SA saída) + FOCO 3 (constants L0).

> **Atualizado 2026-06-15 (sessão AUTOMAÇÃO) — FUNDAÇÃO DO R2 PRONTA + 7 commits anteriores PUSHADOS (ETL deployado).** Item 1 (descoberta automática da remessa) CONCLUÍDO como módulo de produção testado + validado PROD; FLUXO L3 `1.2.4` do R2 desenhado; **TODOS os átomos do R2 capinados** (`montar_invoice_entrada_direta` Skill 7 genérico, `revalorando-custo-odoo` skill nova, picking C9 confirmado). 3 commits NA MAIN (não pushados — dry-run-first, nada os chama ainda): `b697ce93b` `748cd5bd3` `35c1376b7`. **Próximo: (1) WIRE do R2 — service que compõe o FLUXO 1.2.4 end-to-end; (2) SA DURÁVEL da saída (G1+G2) = objetivo final.** Estado anterior (provado): entrada FB CONCLUÍDA no piloto (gate contábil fechado `s67`), LF concluída (2 NFs SEFAZ via SA `s48`–`s59`).

## 🔑 GATILHO DE RETOMADA (colar para retomar)
> **Retomar industrialização FB↔LF — WIRE do R2 (compor o FLUXO L3 1.2.4) + SA DURÁVEL da saída (G1+G2) = objetivo final.** Ler `docs/industrializacao-fb-lf/`: `README.md` (estado) → este handoff (FOCO acima) → `app/odoo/estoque/fluxos/1.2.4-entrada-retorno-industrializacao.md` (desenho do R2, átomos prontos) → `ACHADOS_TECNICOS.md §"R2.3b"` (gate contábil) + §"FASE B" (saída via SA). Desenho/decisões: `SOT_OPERACOES.md §6.2`. **Átomos prontos:** `descoberta_industrializacao.py` (READ) · `escrituracao.py::montar_invoice_entrada_direta` · `revaloracao.py` · Skill 5 picking C9.
>
> **✅ PILOTO 4870112 — CICLO 4+5 COMPLETO (2 NF).** A LÓGICA da SA da entrada está provada e EXECUTADA (`s67`); o que falta é **torná-la durável no Odoo** + automatizar os gatilhos.
> - **Saída (Etapa 4):** 2 NFs de retorno AUTORIZADAS SEFAZ via SA (`s54`): NF-1 791437 `VND/2026/00384` (5124) + NF-2 791441 `RETIN/2026/00001` (16×5902), cstat 100, baixa PASSIVA Δ+279,23, R3 completo.
> - **Entrada (Etapa 5):** **NF-2 MONTADA DIRETO** (in_invoice j1084, 16×op 3252, `calcular_imposto=False`, preços remessa **279,23 exato**) — **refuta o caminho A** (o `criar_invoice` do robô ganha tax lines ESPELHO que auto-cancelam ⇒ não baixa; provado `s66`). **NF-1 serviço 792219** via caminho A (PA via picking C9 325347 done). **Ambas POSTED** (reversível, NÃO SEFAZ).
> - **🔑 DECISÃO Rafael (15/06): gate `PA=Ic+S` MEDIDO PELA CONTA, não pela unidade.** A revaloração `stock.valuation.layer.revaluation` opera sobre o **AVCO do produto inteiro** (593 un do pool) → dilui o Ic (+0,39/un, std 29,87); "305 numa unidade isolada" é incompatível com custo médio (qualquer veículo recalcula a média). Aceito: o **saldo da CONTA de estoque do PA** (`1150100007`) sobe +Ic; a revaloração permanece (fecha a transitória).
> - **GATE CONTÁBIL FECHADO (medido pelo CICLO — NÃO saldo global, que tem tráfego concorrente massivo):** ATIVA `5101010001`=**+0,00** (remessa +279,23 / NF-2 −279,23) · transitória `1150100011`=**−0,00** · estoque PA `1150100007`=**+305,46 (Ic+S)** · refNFe presente · 30720=0.
> - **⚠️ PENDÊNCIA física (não contábil): 26489=−1** (contorno manual do picking C9 `s64`, falta o lado LF→26489) — reconciliar na SA durável.
>
> **✅ FEITO nesta sessão (automação — itens 1+3 fundação):**
> - **Item 1 — descoberta automática da remessa CONCLUÍDA**: `app/odoo/estoque/scripts/descoberta_industrializacao.py` (`DescobertaIndustrializacaoService.descobrir_fonte_nf2(nf1_id)`, READ) — genealogia lote→MO→semis→terceiros (31092) com **rateio faturado/produção_real** (decisão Rafael: rateio, não remessa-inteira; piloto 1/3 PA = R$ 90,89), água excluída, **VALOR via SVL do move de ENTRADA em 31092** (= `price_unit` remessa exato; NÃO o SVL de consumo = AVCO interno), remessa descoberta por voto (picking 322451). 5 testes TDD + validado PROD. Harness: `s69`.
> - **FLUXO L3 `1.2.4`** (`app/odoo/estoque/fluxos/1.2.4-entrada-retorno-industrializacao.md`): desenho do R2 (NF-1 caminho A + picking C9 D-V30-1 + NF-2 montada-direto + revaloração + 26489). Árvore + `scripts/INDEX.md` atualizados.
> - **Átomos do R2 capinados (TDD)**: `montar_invoice_entrada_direta` (Skill 7 `escrituracao.py`, NF-2 montada-direto, **genérico in/out_invoice** — reusa na SA da saída) · `revalorando-custo-odoo` (skill nova `scripts/revaloracao.py`, wizard revaluation, `account_id`=transitória) · `criar_picking_entrada_destino_manual` (Skill 5, já reabilitado — confirmado serve). **16 testes verdes + 79 na suíte escrituração, zero regressão.**
> - **ETL deployado**: os 7 commits anteriores (incl. filtro `company_id not in [5]`) foram **PUSHADOS** (`166d3c078..21ddb3823`) — contaminação do ETL resolvida.
>
> **FOCO DA PRÓXIMA SESSÃO (rumo à SA):**
> 1. **WIRE do R2** — service/orchestração nosso que compõe o FLUXO L3 `1.2.4` end-to-end (busca os 2 DFes → NF-1 caminho A via átomos Skill 7 + picking C9 → `montar_invoice_entrada_direta`+postar NF-2 → `revalorar_custo` → postar NF-1 → reconciliar 26489). Código nosso, construir dry-run-first; **rodar = go fresco por escrita**. Ground: `s63`/`s64`/`s67` (modos). NÃO usar `RecebimentoLfOdooService` (NÃO MEXER) — é só fallback.
> 2. **SA DURÁVEL da saída (G1+G2) = OBJETIVO FINAL.** G1 reusa `montar_invoice_entrada_direta(move_type='out_invoice', operacao_id=2864)` + `DescobertaIndustrializacaoService` (substitui o hardcode da remessa do `s37`); G2 = transmissão via SA (`action_previsualizar_xml_nfe`+`action_gerar_nfe`, provado `s54`). + cron + **script de re-aplicação idempotente versionado** + monitor anti-upgrade. ⚠️ DECISÃO pendente: genealogia DENTRO da SA (server-side, +dívida) vs orchestrator pré-computa via `DescobertaIndustrializacaoService` e grava o spec p/ a SA (RECOMENDADO, −dívida).
> 3. **Reconciliar o trânsito físico 26489** (lado LF→26489) no fluxo automatizado.
> 4. **Demais pontos "1 NF" (`ACHADOS §E`)** — ETL ✅ deployado. Restantes (orchestrator `inventario_pipeline`, Skill 7/8, `recebimento_lf_odoo_service` `invoice_ids[-1]`) são **acoplados ao wire do R2** (o R2 provado NÃO passa pelo `recebimento_lf_odoo_service`); resolver dentro do item 1 do foco. Histórico LF (934 linhas) a limpar à parte.
>
> **🔧 SPEC NF-2 (montada DIRETO — provada `s67 --montar-nf2`):** `account.move` `in_invoice`, partner LF=35, journal **j1084**, `invoice_date`=NF-1; 16 linhas op `3252`+`operacao_manual=True` + **preços da REMESSA** (move 735679, total 279,23); `l10n_br_calcular_imposto=False`; **NÃO** rodar `onchange_l10n_br_calcular_imposto_btn`; R3 = `referencia_ids`[(0,0,{'l10n_br_chave_nf': <chave remessa>})] + `invoice_origin`. A op 3252 leva a conta da linha a **1150100011** sozinha. Postar → `D 1150100011 / C 5101010001 ATIVA 279,23`.
>
> **🔧 AJUSTE DE CUSTO (`s67 --revalorar`):** wizard `stock.valuation.layer.revaluation` (`product_id`=27834, `added_value`=+279,23, `account_journal_id`=8, **`account_id`=1150100011** [a conta que SOBRA na NF-2, NÃO CMV]) → `D 1150100007 PA / C 1150100011`. Dilui no AVCO (aceito); a CONTA do PA sobe +Ic.
>
> **IDs-chave:** FB=1/LF=5 · PA **27834**(4870112) lote **PILOTO-3105**(61519) · **j1084 ENTRI**(no_payment 22800=ATIVA) · op **3252**(1902 entrada, conta linha 1150100011) · op **1917**(serviço) · contas **1150100007** PA / **1150100011** transit / **22800** ATIVA · remessa move **735679** · DFe **44522**(insumos)/**44523**(serviço) · picking **325347** · PO **43464**(NF-1) · team **144** · partner LF **35** · pt52(26489→8) · journal revaloração **8**.
>
> **Estado vivo no Odoo (POSTED, reversível `s67 --reverter <id_nf2>`):** NF-1 **792219** posted · NF-2 **795439** posted (`ENTRI/2026/06/0001`) · picking 325347 done · PO 43464 · j1084.
>
> **Commits desta sessão (automação) — na MAIN, NÃO pushados** (átomos dry-run-first, nada os chama em prod ainda): `b697ce93b` (descoberta + fluxo 1.2.4) · `748cd5bd3` (`montar_invoice_entrada_direta`) · `35c1376b7` (`revalorando-custo-odoo`) [+ commit de docs]. **Os 7 commits anteriores (filtro ETL `43127eb76` + entrada `76a44db6b` + G9) JÁ PUSHADOS** (`21ddb3823`, deploy Render) — contaminação do ETL resolvida.
>
> **🔴 Regras invioláveis:** **dry-run + go FRESCO do Rafael em CADA escrita Odoo**; nada postado sem go; reversível (j1084/j1001 `restrict_mode_hash_table=False`); toda transmissão SEFAZ é PRODUÇÃO (`tpAmb=1`), IRREVERSÍVEL. **Medir SEMPRE pelo LANÇAMENTO do ciclo, nunca pelo saldo global** (contas compartilhadas pela empresa inteira).

## Como começar
Leia nesta ordem: **`README.md`** (índice+estado) → o **GATILHO DE RETOMADA acima** → **`ACHADOS_TECNICOS.md §"R2.3b"`** (gate da entrada fechado) + §"FASE B" (saída via SA). Desenho/decisões (dona): **`SOT_OPERACOES.md §6.2`** (automação dos 2 gatilhos, v3.4) + §6 requisitos.
**JÁ CONCLUÍDO — NÃO refazer:** Etapas 1-3 (remessa / entrada LF / MO) + **Etapa 4 (retorno LF→FB)** (2 NFs autorizadas SEFAZ via SA) + **Etapa 5 (entrada FB) no piloto** (2 NFs escrituradas/posted, gate contábil fechado `s67`). **O que falta é a AUTOMAÇÃO (SA durável + gatilhos), não a prova do fluxo.**

> ⬇️ **Abaixo = HISTÓRICO do desenho (a emissão das 2 NFs JÁ foi implementada e a LF firmada — ver o gatilho de retomada no topo).** Mantido como contexto/arqueologia.

## A DECISÃO (aprovada 2026-06-02) — ponto de partida da implementação
A Contadora **APROVOU emitir o retorno em 2 NF** — o retorno de insumos (5902/1902) em **documento separado** do serviço (5124/1124) — com **3 REQUISITOS invioláveis**:
1. **R1 — emissão da NF de retorno AUTOMÁTICA:** a 2ª NF (insumos) deve sair **automaticamente, do mesmo jeito que os componentes são incluídos na NF hoje** (= expansão da BoM do PA). Zero digitação manual de insumos.
2. **R2 — escrituração da ENTRADA AUTOMÁTICA:** o DFe da NF de retorno deve ser escriturado automaticamente na FB **junto com** a NF de industrialização.
3. **R3 — VÍNCULO entre as 2 NFs** (retorno ↔ industrialização) para rastreabilidade.

⇒ Caminho **(b) separar = confirmado**. **Forma 1 (subcontratação nativa) DESCARTADA** (configurada mas dormente — `ACHADOS §I`). **Forma 2 (nosso pipeline deriva a 2ª NF) é o caminho.**

## Onde estamos (piloto 1 caixa, produto 4870112, lote PILOTO-3105)
- ✅ Etapas **1 → dreno físico → 2 → E** (remessa `RPI/2026/00245` SEFAZ-OK; entrada LF Model B ENTIN 737062; MO 20252+20254; PA em 31093). **Faltam Etapa 4 (faturar retorno) + Etapa 5 (escriturar FB).**
- ✅ Sessões 5–8: mecanismo do retorno **totalmente mapeado e provado** (READ-only + experimentos NF-teste postada/excluída; **zero escrita definitiva no Odoo**). Sessão 8 fechou a engenharia da emissão (hipótese (i) refutada → split pré-SEFAZ) e armou o GATE 0.
- ✅ **Estado ao vivo re-validado 12/06** (`s8c`/`s8d` dry-run): PA 1 un em 31093 lote PILOTO-3105 livre; NF-modelo 180552 intacta; nenhum journal sale LF com no_payment=26667 (gap confirmado).

## O QUE JÁ FOI PROVADO — NÃO refazer (`ACHADOS §A–§I`)
- 1 NF mista **NÃO baixa** a compensação (8 ângulos + R-UNIF nos 2 lados). Caminho = **2 documentos**.
- A separação é de **COMPOSIÇÃO de linhas**, não de movimento físico: insumos 5902/1902 **já são simbólicos** (0 stock.move); só o **PA** tem move, na **linha de serviço 5124↔1124**.
- **Fonte dos insumos = BoM do PA** (9/9 batem) → a 2ª NF é **100% derivável** ⇒ **R1 é viável** (já explodimos a mesma BoM na remessa, `RUNBOOK §1`).
- **Vínculo nativo existe:** `account.move.referencia_ids` (refNFe) na saída; cadeia `DFe→PO→invoice` na entrada (3087 casos reais) ⇒ **R2/R3 viáveis** (entrada já é automática).
- Operador cria **1 picking (só o PA)**; o robô expande a BoM via `stock.invoice.onshipping.create_invoice` (`journal = picking_type.l10n_br_tipo_pedido`; 1 picking = 1 account.move).

## PRÓXIMA SESSÃO = IMPLEMENTAR (investigar a engenharia da automação, nesta ordem)
1. **R1 — emitir a 2ª NF (saída LF).** ✅ **DESENHO E2E FECHADO na sessão 9 (12/06): `SOT §6.1 v3.1`** (🟡 proposta, aguarda validação do Rafael) — **wizard nativo `stock.invoice.onshipping` chamado por NÓS** (mesma chamada do robô ⇒ expansão automática da BoM = R1 literal) **+ split em DRAFT** (NF-serviço só-5124 no j847; NF-insumos só-5902 no journal RETIND com `l10n_br_no_payment=True` + `no_payment=26667` + price_unit FORÇADO = remessa) + saga de transmissão (serviço → refNFe → insumos). Histórico: hipótese (i) refutada na sessão 8 (`create_invoice` funde; VIA 1 fechada). Desenho revisado por painel adversarial 3 lentes (30 findings incorporados — CST 50/51 corrigido, pt98 pré-req, ETL pré-condição GATE 2, 3 alavancas anti-robô).
   - ✅ **GATE 0 EXECUTADO E APROVADO (2026-06-13)** (`scripts/s8d_gate0_split_experimento.py` v3.1): split contábil em journals de teste **provou a hipótese central** — NF só-5902 com journal `l10n_br_no_payment=True`+conta 26667 **baixa a PASSIVA `5101020001`** (`D 8,37`, total=0) no `action_post`; NF só-5124 fica limpa. Sem SEFAZ, postado e deletado (zero rabo; NF-modelo 180552 intacta). **2 aprendizados** (`ACHADOS §"GATE 0 EXECUTADO"`): (a) o redirecionamento do no_payment só aparece pós-`action_post` (draft mostra CLIENTES) — não assustar no GATE 1; (b) resíduo = a conta de contrapartida das 5902 (transitória 1150100012 vs terceiros 1150200001) — definida pela OPERAÇÃO fiscal da linha, a verificar no GATE 1.
   - ✅ **VEÍCULO DECIDIDO: SERVER ACTION** (Rafael 13/06) — server-side mata o timeout do recompute; gatilho via cron (padrão dos 12 robôs); ⚠️ dívida: script de re-aplicação versionado + monitor anti-upgrade (some em upgrade CIEL IT); SEFAZ fica fora da SA (Playwright nosso). `SOT §6.1` "Veículo".
   - ✅ CONFIG: journal **RETIND id=1083** criado. ⚠️ pt98 **revertido** (GATE 1 provou que é pt66→Clientes, não pt98→26489).
   - ⚠️ **GATE 1 EXECUTADO (13/06, `s11_gate1_emissao.py`) — tudo revertido, shoyu restaurado, PA em 31093.** **3 camadas** (`ACHADOS §"GATE 1 EXECUTADO"`): (1) picking de retorno = **pt66 src override 31093→dst 5 Clientes** (pt98/26489 falha a operação fiscal); (2) **🔑 `create_invoice` via XML-RPC exige `allowed_company_ids=[5]` LF-ONLY** (`[1,5]` pega income FB → "empresas incompatíveis"); recompute 48s; a **NF do PA (5124) sai correta**; (3) **🔴 a expansão automática dos 5902 NÃO ocorre p/ o shoyu** — **investigado/REFUTADO** (criei BoM subcontract 16-comps + rota Comprar-FB 5 + conferi supplierinfo LF → nada destravou); causa = comportamento fiscal profundo CIEL IT (`l10n_br_compra_indcom`=`uso` no shoyu vs `ind` no azeite; mesma op 2702; caixa-preta — o shoyu provavelmente nunca foi cadastrado como industrialização no CIEL IT).
   - 🔑🔑 **GATE 1b — DESCOBERTA DEFINITIVA (`ACHADOS §"GATE 1b"`): a expansão dos 5902 é MANUAL, não automática.** Testei o azeite (que "expande" no robô) → o nosso `create_invoice` via XML-RPC **e** uma **server action de teste** (sudo+onchange, autorizada pelo Rafael, deletada) **NÃO expandiram** (1×5124, 0×5902). Investigando a NF real `738097`: a linha 5124 foi criada pelo **robô/OdooBot** (17:00:57); as **9 linhas 5902 pela OPERADORA Josefa** (uid 38) **12 min depois** (17:12:18, todas no mesmo instante = botão/wizard). ⇒ **NÃO existe expansão automática** — o robô cria só o PA, a operadora adiciona os insumos à mão. **REFUTA a sessão 7 §G.** Toda a investigação anterior (pt66 OK e CTX LF-only continuam válidos; mas BoM subcontract/rota/compra_indcom/server-side foram beco — buscavam um mecanismo inexistente).
   - ⇒ **CONCLUSÃO: a Forma 2 é o caminho; o R1 da Contadora ("do mesmo jeito que hoje") pede AUTOMATIZAR o passo MANUAL da Josefa.** Nós criamos as 16 linhas 5902 (op **2864**, CST **50**, conta **26855**, price da remessa) via XML-RPC (computes rodam no write server-side) → **não precisa server action p/ a expansão**. Veículo (server action vs pipeline) volta a ser só de orquestração.
   - ✅ **CONFIRMADO 100% MANUAL (13/06):** em 30 VND mistas, as **262 linhas 5902 foram TODAS criadas por operadoras** (Josefa uid 38 / Histaina uid 1250), **zero pelo robô**. Não há botão/wizard CIEL IT (só transmissão/impostos na view) — as operadoras editam o grid da NF à mão. Doc do projeto não tem o método (Explore confirmou).
   - ✅ **DECISÃO (Rafael): plano B (Forma 2) via SERVER ACTION** (não XML-RPC — server-side garante os computes fiscais como o robô).
   - ✅ **GATE 1c PROVADO (13/06, `s15_gate1c_montar_5902.py`):** server action de escrita (autorizada Rafael, deletada) **automatiza o passo manual da Josefa** — `create_invoice` (NF do PA) + explode a **BoM subcontract** + cria as N linhas 5902 (`l10n_br_operacao_id=2864` + product + quantity + price; `check_move_validity=False`) + recompute server-side. **Azeite: NF de 10 linhas (1×5124 + 9×5902), 0 erros, CST 51/50 — IDÊNTICA à NF real da Josefa.** ⇒ Forma 2 via server action VIÁVEL (a op 2864 na linha faz o CIEL IT computar cfop 5902/CST 50/conta). Revertido (azeite 4 em 42).
### 🔒 ESTRATÉGIA FIRMADA (Rafael, 2026-06-13 tarde) — construir a solução-alvo
> **Mudança de abordagem:** parar de investigar "como é hoje" (a inserção manual das 5902 — provado em `ACHADOS §"SESSÃO 2026-06-13 (tarde)"` que **não há** server action/automação/botão/wizard; é a operadora). Construir **1 server action que gera as 2 NFs**, partindo do que já está pronto (regra Rafael: se existe e muda pouco → alterar; se gasta muito p/ achar e dá p/ criar → criar).
> **Estado limpo** (azeite 4 em 42, shoyu 1 em 31093, RETIND 1083 + op 3252 mantidos; pt98 revertido; 0 SA/NF de teste). Scripts READ desta sessão: `s16`–`s23` (ver ACHADOS).

**ALVO — SA "Gerar Retorno Industrialização" (gera 2 NFs):**
1. **NF-1 SERVIÇO (5124)** — `create_invoice` do picking do PA (CTX LF-only `allowed_company_ids=[5]`) → journal j847, 1 linha op 2702 CST 51. (= GATE 1c, já provado.)
2. **NF-2 INSUMOS (5902)** — `account.move` NOVO no **RETIND 1083** (partner FB=1, out_invoice) + N linhas op **2864** (`check_move_validity=False`) + recompute server-side → CST 50, baixa **PASSIVA 5101020001**. (= `s18` + GATE 0, já provados; **trocar a fonte**.)
   - **🔑 FONTE = MATERIAIS DE TERCEIROS via MO/quant** (não BoM teórica, não remessa): explodir a estrutura de produção do PA (MO + sub-MOs) pegando o consumido de `31092 LF/Materiais de Terceiros` → exclui **ÁGUA** (consumo local) e **semis** ⇒ os 16 corretos. (BoM teórica pura dá 17 c/ água — `ACHADOS §"FONTE = MATERIAIS DE TERCEIROS"`.)
   - `price_unit` = valor de remessa (invariante 5902=5901); refinar no piloto.
3. **R3 — vínculo** `referencia_ids` (refNFe) + `invoice_origin` comum entre NF-1 e NF-2.
4. **R2 — escrituração FB** dos 2 DFes juntos (trilho `DFe→PO→invoice`, 3087 casos).
5. **SEFAZ FORA da SA** (Playwright nosso, saga/rollback).

**TRIBUTAÇÃO (regra Rafael 13/06):** NADA tem ICMS; **só a NF de serviço (5124) tem PIS/COFINS** (CST 01); o **retorno 5902 NÃO tem ICMS/PIS/COFINS** (`tax_ids=[]`; CST 50/08 = só codificação). Detalhe: `ACHADOS §"TRIBUTAÇÃO — REGRA DEFINITIVA"`.

**✅ NF-2 (insumos) — MONTAGEM PROVADA (`s24`, 2026-06-13):** a SA monta a NF-insumos do shoyu no RETIND 1083 — 16×5902 (BoM `type=product`, MATCH EXATO vs NF real 709632), **estrutura idêntica: cfop 5902 / CST 50 / conta 1150100012**. ✅ **TRIBUTAÇÃO RESOLVIDA (`s31`–`s33`):** o `amount_tax=−278,17` NÃO é imposto — é a **baixa da PASSIVA 5101020001** (`tax_line_id` vazio, conta 26667, D=valor cheio); tributo real nas linhas = 0 (igual à NF real); `l10n_br_total_nfe`(vNF SEFAZ)=valor cheio (igual à SARET real autorizada). NF-2 fiscalmente pronta; só falta `price_unit`=remessa + medir baixa no post. Mecanismo: header (`tipo_pedido`+`operacao 2864`+`fp`) + linhas `operacao_manual=True` + map de conta explícito da fp 111 (`account.fiscal.position.account`) porque a fp do move reseta e `map_account` é sobrescrito (CIEL IT). Detalhe: `ACHADOS §"GATE Forma B"`.

**PRÓXIMOS PASSOS (em ordem):**
1. ~~Zerar o imposto da NF-2~~ ✅ **RESOLVIDO** (`s31`–`s33`, 2026-06-13 noite): **não havia imposto** — o "−278,17" é a **baixa da PASSIVA** 5101020001 (contrapartida `no_payment`, `tax_line_id` vazio). NF-2 fiscalmente pronta: tributo 0, cfop 5902 / CST 50 / conta 1150100012, vNF SEFAZ = valor cheio. A "limpeza de tax line" do `s30` fica **fora da SA final** (removeria a baixa).
2. ~~`price_unit` real = valor da remessa~~ ✅ **FEITO** (`s34`/`s35`): fonte = linhas product da remessa **RPI/2026/00245 (move 735679)** — 16 insumos exatos, qty+price prontos, total **279,23** = untax remessa (MATCH com NF real 709632).
3. ~~Postar a NF-2 → medir baixa~~ ✅ **FEITO** (`s35 --postar`): post no RETIND 1083 → **baixa da PASSIVA 5101020001 = Δ+279,23** (D na conta 26667 = valor dos insumos). Journal `hash=False` ⇒ revertido (`--cleanup`), saldo restaurado. **NF-2 isolada 100% provada.**
4. **⬅️ PRÓXIMO: Unir NF-1 (serviço 5124 via create_invoice) + NF-2 (insumos, `s35`) na MESMA server action** + vínculo R3 (`referencia_ids`/`invoice_origin` comum). (NF-1 = GATE 1c já provado; NF-2 = `s35` já provado; falta orquestrar as duas + R3.)
5. GATE 2 (SEFAZ, irreversível, go duplo) — transmissão via Playwright (fora da SA).
6. Entender por que `fiscal_position_id` reseta p/ False no recompute (workaround atual = map explícito).

**Pendências de fundo:** **dívida server action** = script de re-aplicação idempotente versionado + monitor anti-upgrade (SA custom some em upgrade CIEL IT); medir baixa PASSIVA end-to-end; gatilho da SA (sobre a MO / picking / NF-serviço gerada — decidir).
2. **Journal + no_payment (config):** criar/repontar **1 journal de retorno-de-insumos** (LF sale) com `no_payment=26667` (PASSIVA `5101020001`) — hoje **NENHUM** journal sale LF aponta a PASSIVA (j1002 RETRABALHO usa 5101010046; mudá-lo atinge todo o retrabalho). Lado FB: journal de entrada-de-insumos com `no_payment=22800` (ATIVA) ou ajustar j1001. *(CFOP 5902/1902, não 5949 — confirmar fp que força 5902.)*
3. **R3 — vínculo:** preencher `referencia_ids` (refNFe) automaticamente nas 2 NFs de retorno (apontando remessa RPI/5901 e/ou uma à outra) + mesmo `invoice_origin`.
4. **R2 — escrituração automática (entrada FB):** estender `DFe→PO→invoice` para os 2 DFes (serviço + insumos) escriturarem **juntos e vinculados** (o trilho já roda em 3087 casos).
5. **G8 (AVCO Ic+S):** medir no piloto como o PA recebe Ic+S com a 1902 simbólica (op 3252, `mov_estoque=False`) — `ACHADOS §D` (resíduo a fechar só no ciclo real).
6. **Pontos de código que assumem "1 NF":** ajustar conforme `ACHADOS §E` (orchestrator `inventario_pipeline`, Skill 8 `faturamento.py`, Skill 7 `escrituracao.py`, `recebimento_lf_odoo_service`, ETL `faturamento_service` — filtrar a NF de insumos total=0 p/ não re-baixar carteira).
7. **Piloto 4870112 (ciclo 4+5 com 2 NF):** gate de sucesso — `Δ5101020001(LF)=0`, `Δ5101010001(FB)=0`, PA por `Ic+S`, 26489=0, 30720=0, **vínculo refNFe presente nas 2 NFs**.

## Regras invioláveis
- **dry-run + "go" FRESCO do Rafael em CADA escrita Odoo** (go DEPOIS da dry-run). NF SEFAZ (irreversível) só com go explícito. 1 comando por escrita.
- Experimentos Odoo: NF-teste em journal de teste, postar só se necessário, **SEMPRE limpar depois** (deletar NF + journal — "não sobrar rabo").
- **NUNCA** `action_apply_inventory` cru → Skill 1. Ops LF: contexto `allowed_company_ids=[1,5]`.

## IDs/constantes-chave
Empresas FB=1/LF=5 · partner LF=35 · trânsito 26489 · 31092 (LF/Mat.Terceiros) · 31093 (LF/PA Terceiros) · 30720 (FB customer terceiros) · pt98 (LF saída retorno 31093→26489, `tipo_pedido=False`) · pt66 (LF Exp.Industrializacao `venda-industrializacao`→j847) · pt97 (LF Exp.Ind.Retorno `dev-industrializacao`→j1002) · pt52 (FB entrada retorno src=26489).
Contas: 5101010001 ATIVA FB=**22800**/LF=26652 · 5101020001 PASSIVA FB=22815/LF=**26667**.
Journals: j1001 ENTSI FB-purchase (no_pay VAZIO) · j1047 ENTRADA-REMESSA LF-purchase (no_pay 26667) · j847 VENDA PRODUÇÃO LF-sale (NF mista retorno; no_pay VAZIO) · j1002 RETRABALHO LF-sale (`dev-industrializacao`, no_pay 26863=5101010046) · j1003 PERDAS LF-sale (5903) · **`j1083` RETIND LF-sale (`no_payment=True`+26667 PASSIVA, tipo_pedido vazio) — CRIADO 13/06, destino das 5902 da NF-insumos (preenche o gap R1/journal)**.
Operações: 5124→2702 · 5902→2864 (NF mista) / **2710 (dev-ind, separa)** · 5903→2711. FB entrada: 1124→**1917** · 1902→2027 (mov_estoque=True, double-count) / **3252 (mov_estoque=False, mata double-count)**. fp retorno LF=**111**; fp entrada FB serviço=**88**.
BoM do PA: `normal` + `subcontract` (subcontratante=LF) — a fonte dos insumos 5902. Lote FB PILOTO-3105 ids 60496-60511 (company 1, imutável).

> **🔢 TAREFA DEDICADA (sessão limpa) — consolidar estes constants em L0** (`app/odoo/constants/` — `locations.py` · `picking_types.py` · `operacoes_fiscais.py` · `ids_diversos.py` + ev. `industrializacao.py` novo) + atualizar `.claude/references/odoo/IDS_FIXOS.md`. Hoje estão hardcoded espalhados nos `s37`/`s54`/`s63`/`s67`/`s69` + átomos + fluxo 1.2.4.
> **🔴 GUARDRAIL (por que sessão limpa, não no fim de um contexto longo):** são IDs FISCAIS — `account.account` **é company-specific** (5101010001 = 22800 FB **≠** 26652 LF; 5101020001 = 22815 FB ≠ 26667 LF; 1150100007 PA FB=22294; 1150100011 FB=26842). Op/journal errado = dano real quando a SA rodar. **VERIFICAR CADA id contra o Odoo VIVO (read-only) antes de gravar em L0** (Zero Invenção). O inventário acima é a fonte; a consolidação cross-checa e nomeia. Cadastro saída (gaps `s37`): incoterm CIF=6 · carrier LF=999 · payment_provider LF=31 · vencimento a-prazo. Constants FB R2: payment_term 2791 · picking_type 1 · payment_provider 38 · team 144 (G039 dinâmico). journal revaloração=8.

## Contador — status
✅ **APROVADO emitir 2 NF (2026-06-02)** + 3 requisitos (R1/R2/R3) + Etapas 4-5 + Opção A (Ativo→Ativo, CPV só na venda) + PA=Ic+S. **Sem gargalo fiscal restante.** Separado/sem prazo: regularização dos acumulados (5101010001 R$60,8M FB; insumos sem baixa R$8,68M LF — `GOALS G9`).
