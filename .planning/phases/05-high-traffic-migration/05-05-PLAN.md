---
phase: 05-high-traffic-migration
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All migrated templates work in dark mode (visual inspection)"
    - "All migrated templates work on mobile (no horizontal overflow)"
    - "Print templates render correctly on paper (white background, black text)"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint for Phase 5 migration

Purpose: Human verification that all migrated templates work correctly in both themes and on mobile
Output: Confirmation that Phase 5 success criteria are met
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-high-traffic-migration/05-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Migrated three high-traffic modules to design system:
    1. **Financeiro**: dashboard.html, cnab400_hub.html, cnab400_lote_detalhe.html
    2. **Carteira**: dashboard.html, agrupados_balanceado.html
    3. **Embarques**: listar_embarques.html, visualizar_embarque.html

    Print templates (imprimir_*.html) intentionally kept with hardcoded colors.
  </what-built>
  <how-to-verify>
    **Dark Mode Verification:**
    1. Open browser, navigate to /financeiro/dashboard
    2. Toggle dark/light mode using theme switch
    3. Check: All cards, text, and badges adapt correctly
    4. Repeat for:
       - /financeiro/cnab400 (CNAB hub)
       - /carteira (dashboard)
       - /carteira/agrupados (main carteira view)
       - /embarques (list view)

    **Mobile Verification:**
    1. Open DevTools (F12) > Toggle device toolbar (Ctrl+Shift+M)
    2. Select "iPhone 12 Pro" or similar mobile preset
    3. For each page above, check:
       - No horizontal scroll on entire page
       - Tables have horizontal scroll within container
       - Action buttons are accessible (44x44px minimum)
       - Text is readable

    **Print Template Verification:**
    1. Navigate to an existing embarque detail
    2. Click print button or use Ctrl+P
    3. In print preview, verify:
       - White background
       - Black text
       - No dark mode colors
       - Layout fits on paper

    **Specific Checks:**
    - [ ] Financeiro dashboard: Section cards have correct background in both themes
    - [ ] CNAB400 hub: Stat cards and status badges readable in both themes
    - [ ] Carteira agrupados: Filter badges and table rows adapt to theme
    - [ ] Embarques list: Table rows and badges adapt to theme
    - [ ] Print preview: White background, black text, readable layout
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass.

    If issues found, describe:
    - Which template/page has the issue
    - What the issue is (e.g., "white text on white background in dark mode")
    - Screenshot if possible
  </resume-signal>
</task>

</tasks>

<verification>
Human visual inspection completed. Phase 5 success criteria validated:
1. Financeiro dashboard and child templates use tokens (no hardcoded colors)
2. Carteira templates use tokens for colors while preserving layout
3. Embarques templates including print views work in both themes (print in light only)
4. All migrated templates pass dark mode visual inspection
5. All migrated templates work on mobile
</verification>

<success_criteria>
User confirms:
- All dark mode checks pass
- All mobile checks pass
- Print template renders correctly
- No visual regressions from before migration
</success_criteria>

<output>
After completion, create `.planning/phases/05-high-traffic-migration/05-05-SUMMARY.md`
</output>
