---
date: 2026-06-09
suggestions_evaluated: 2
implemented: 1
rejected: 0
proposed: 1
---

# Improvement Dialogue — 2026-06-09

## Sugestoes Avaliadas

### [RESPONDIDO/IMPLEMENTADO] IMP-2026-06-08-001: Lancamento CTe/despesa no Odoo sem lock de re-entrada — gera POs e invoices duplicados
- **Categoria**: skill_bug
- **Severidade**: warning
- **Decisao**: respondido (implementado — Acao B)
- **Implementado**: sim (auto, no worker)
- **Arquivos afetados**:
  - `app/fretes/workers/lancamento_odoo_jobs.py`
  - `tests/fretes/test_lancamento_lock.py` (novo)
  - `tests/fretes/__init__.py` (novo)
  - `app/fretes/CLAUDE.md` (R7 reescrita)
- **Notas**:
  - **Bug confirmado contra o codebase.** Os jobs `lancar_frete_job` (linha ~156) e `lancar_despesa_job` (~376) em `app/fretes/workers/lancamento_odoo_jobs.py` nao tinham lock distribuido — lacuna ja **documentada explicitamente** na regra R7 do `app/fretes/CLAUDE.md` ("Sem Redis lock no lancamento individual").
  - **Vetor da race**: a Etapa 6 `_gerar_po_dfe_fire_and_poll` -> `action_gerar_po_dfe` (`lancamento_odoo_service.py:509`/`:563`) demora 60-90s+. O unico guard era `_buscar_po_por_dfe` (`:531`), que consulta o estado do Odoo mas tem janela de race de ~1s: duas execucoes paralelas do mesmo item passam ambas e criam 2 POs + 2 invoices. Caso real: CTe 135210 (TALITA, 2 cliques com 9s) -> PO C2620079 + C2620082, invoices COM2/2026/06/0090 + 0091 (correcao manual 08/06).
  - **Fix (auto-implementavel, no worker — NAO toca models/routes/client)**: helpers `_adquirir_lock_lancamento(tipo, id)` / `_liberar_lock_lancamento(tipo, id)` usando `redis.set(nx=True, ex=900)` com fail-open, espelhando o padrao ja validado em `app/recebimento/workers/recebimento_lf_jobs.py`. Chaves `lancamento_frete_lock:{frete_id}` e `lancamento_despesa_lock:{despesa_id}`. Aquisicao no inicio do job (antes do `app_context`); liberacao garantida no `finally`. Se o lock ja esta retido, o job aborta com `error_type='LANCAMENTO_EM_ANDAMENTO'` + `skipped=True`, sem abrir contexto Flask nem tocar o Odoo. TTL 900s > timeout do job (600s) + polling da Etapa 6; expira sozinho se o worker morrer.
  - **Lote**: `lancar_lote_job` apenas enfileira jobs individuais — herda o lock de cada um, sem lock proprio (escopo minimo).
  - **Descopado (proposto)**: deduplicacao no nivel da ROTA (`routes.py:1018` `/lancar-odoo`) NAO foi implementada — `routes.py` e arquivo core proibido para auto-implementacao. O lock no worker ja serializa o caso real (mesmo que a rota enfileire 2 jobs, o 2o e negado). Bloquear ja no `enqueue_job` fica como melhoria de UX para revisao humana.
  - **Testes**: 9 casos TDD em `tests/fretes/test_lancamento_lock.py`, todos verdes (aquisicao, negacao na 2a vez, namespaces independentes frete/despesa, liberacao+readquirir, fail-open com Redis None e com excecao, abort do job com lock ativo sem abrir contexto Flask, liberacao no `finally` apos erro dentro do contexto).
  - **Gate A3 (Passo 3.5)**: nao se aplica — a mudanca nao toca nenhum subagente com golden dataset (analista-carteira, auditor-financeiro, controlador-custo-frete, gestor-motos-assai), nem skill/reference/regra de negocio coberta por eles.

### [RESPONDIDO/PROPOSTA] IMP-2026-06-08-002: CarVia fretes — sem skill de escrita (fallback para Bash+psycopg2)
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Decisao**: respondido (proposta para revisao humana — Acao C)
- **Implementado**: nao
- **Arquivos afetados**: nenhum
- **Notas**:
  - **Skill `atualizando-frete-carvia` nao existe** (confirmado: a unica skill com "carvia" no nome e `gerindo-carvia`, READ-only, `allowed-tools` sem Write/Edit). Sugestao valida em principio, mas **NAO auto-implementada** por 3 motivos:
    1. **Frequencia**: `occurrences=1` (1 sessao). Skill nova de WRITE para 1 caso = ROI baixo / risco alto.
    2. **Invariantes complexas**: escrita CarVia tem regras nao-triviais (`app/carvia/CLAUDE.md`) — R3 (apos alterar `peso_bruto`/`peso_cubado` e OBRIGATORIO chamar `operacao.calcular_peso_utilizado()`; `peso_utilizado` e **calculado**, nao update direto, e a cotacao usa esse valor), R4 (state machine — nunca mover status para tras), R14/R14.1 (cancelamento atomico em ordem topologica). UPDATE cru furaria essas invariantes.
    3. **Risco financeiro**: `valor_cotado`/`valor_cte`/`valor_acertado` afetam conferencia de fatura.
  - **Correcao de campos na sugestao** (verificado vs schemas): a sugestao mistura campos de tabelas diferentes. `carvia_fretes` tem `peso_total`/`valor_cotado`/`valor_cte`/`valor_considerado` (NAO tem `valor_acertado`). `carvia_subcontratos` tem `valor_cotado`/`valor_acertado` (NAO tem os outros). `carvia_operacoes` nao tem nenhum — usa `cte_valor` e `peso_utilizado` (calculado). Uma futura skill precisa rotear campo->tabela corretamente.
  - **Plano (se aprovado)**: criar `.claude/skills/atualizando-frete-carvia/` com `SKILL.md` + `scripts/atualizar_frete_carvia.py` no padrao WRITE (dry-run DEFAULT, `--confirmar`, `--user-id`, output JSON, exit codes 0/1/4/5). O script deve **delegar aos services de dominio CarVia** (que aplicam R3/R4), nunca UPDATE cru. Registrar em ROUTING + tool_skill_mapper + cross-refs (padrao skill completo).
  - **Gotcha "replica de leitura tem IDs diferentes" — NAO documentado**: a explicacao e tecnicamente **suspeita**. Streaming replication do Postgres e copia fisica byte-a-byte — IDs de sequencia NAO divergem entre primario e replica (um id=364 no primario e id=364 na replica, ou ainda nao existe la por lag = 0 rows, nao id diferente). A observacao real (UPDATE 0 rows; id via MCP != id no psycopg2) tem causas-raiz mais provaveis a **investigar** antes de virar doc: (1) MCP `query_render_postgres` e Bash+psycopg2 do agente apontavam para BANCOS diferentes (`DATABASE_URL` distintos); (2) lag de replicacao com registro recem-criado. Documentar a versao da sugestao propagaria imprecisao tecnica (viola Zero Invencao).

## Resumo
- **Total avaliadas**: 2
- **Implementadas automaticamente**: 1 (IMP-2026-06-08-001 — lock Redis de re-entrada, 9 TDD verdes)
- **Rejeitadas**: 0
- **Propostas para revisao humana**: 1 (IMP-2026-06-08-002 — skill WRITE CarVia + gotcha replica precisa investigacao de causa-raiz)
- **Persistencia DB**: OK (v2 IDs 132, 133, HTTP 200)
- **Gate A3**: nao aplicavel (nenhum subagente com golden dataset afetado)
