---
date: 2026-06-17
suggestions_evaluated: 4
implemented: 2
rejected: 0
proposed: 2
---

# Improvement Dialogue — 2026-06-17

## Sugestoes Avaliadas

### [IMPLEMENTADO] IMP-2026-06-16-002: operando-picking-odoo — idempotencia de 'devolver' reutiliza picking de devolucao CANCELADO
- **Categoria**: skill_bug
- **Severidade**: warning
- **Decisao**: respondido (auto-implementado)
- **Implementado**: sim
- **Arquivos afetados**:
  - `app/odoo/estoque/scripts/picking.py` (metodo `StockPickingService.devolver()`)
  - `.claude/skills/operando-picking-odoo/scripts/operar_picking.py` (`devolver_single()`)
  - `tests/odoo/services/test_stock_picking_service.py` (2 testes novos)
- **Notas**: BUG REAL confirmado lendo o codigo. A busca de idempotencia
  (`origin ilike "Devolução de NAME"`) NAO filtrava `state`, entao uma
  devolucao antiga em `state=cancel` (move qty=0, nao restaura saldo) era
  reutilizada — o dry-run retornava `reutilizado_idempotente=true` apontando
  para o picking MORTO e o real-run retornava `DEVOLUCAO_REUTILIZADA` sem
  restaurar estoque. Incidente: picking 325359 (FB/SAI/IND/01633), devolucao
  cancelada 325674, travou reversao de 5.000 un lote 26329.

  O codigo do fluxo oficial ERRA → BUG REAL (matriz §2.1.1: leitura do codigo
  e soberana, sinal externo irrelevante). O mesmo padrao ja estava codificado
  em `criar_picking_inter_company` (G-AUDIT-3/N23, `picking.py:969-1006`) com a
  licao atemporal "idempotencia por chave externa SEMPRE filtra state=cancel".
  Aplicada a MESMA segregacao em `devolver`: le `['id','state']`, separa vivas
  (draft/confirmed/assigned/done) de canceladas; reutiliza a viva se existir,
  cria NOVA se so houver canceladas, e loga as canceladas ignoradas. Corrigido
  nos 2 pontos (service real-run + CLI dry-run). Suite: **72 passed** (70
  baseline + 2 testes novos), py_compile OK nos 2 scripts.

### [PROPOSTO] IMP-2026-06-16-003: Skill para adicionar separacao em embarque existente
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Decisao**: respondido (proposta — revisao humana)
- **Implementado**: nao
- **Arquivos afetados**: nenhum (plano em implementation_notes)
- **Notas**: Sugestao valida — nao existe caminho pronto. A rota `novo_item`
  (`app/embarques/routes.py:1163`) existe mas e incompleta (cria `EmbarqueItem`
  sem `separacao_lote_id`/`cnpj_cliente`/`peso`/`valor`/`pallets` e nao recalcula
  totais). NAO auto-implementada porque: (a) o caminho recomendado toca
  `routes.py` (core file, restrito ao D8 = apenas propor); (b) criar skill WRITE
  dedicada para o agente web e feature grande que merece decisao humana;
  (c) frequencia = 1 sessao. Plano: estender `novo_item` aceitando
  `separacao_lote_id`, preenchendo campos da `Separacao` + UF/cidade via
  `LocalizacaoService.obter_cidade_destino_embarque()` (`app/utils/localizacao.py:373`)
  e chamando `sincronizar_totais_embarque()`
  (`app/embarques/services/sync_totais_service.py:47`) — NAO UPDATE manual de totais.

### [PROPOSTO] IMP-2026-06-16-004: conciliando-transferencias-internas — cobrir "substituir perna em journal errado"
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Decisao**: respondido (proposta — revisao humana + staging)
- **Implementado**: nao
- **Arquivos afetados**: nenhum (plano em implementation_notes)
- **Notas**: Situacao real e nao coberta (skill cobre Situacoes 1/2/2b). NAO
  auto-implementada porque o fluxo envolve operacoes financeiras DESTRUTIVAS no
  Odoo PROD nao testadas (desfazer `full_reconcile`, `action_cancel` de payment
  postado, re-reconciliar). Pre-requisitos a validar antes: comportamento do
  `action_cancel` em `account.payment is_internal_transfer` nesta instalacao
  CIEL IT. O gotcha citado ("writes exigem app_context Flask senao
  odoo_audit_hook faz rollback") e REAL — o hook existe em
  `app/odoo/utils/connection.py` (P8, flag `AGENT_ODOO_AUDIT_HOOK`, registra em
  `operacao_odoo_auditoria`) — e deve ser documentado na skill. Escopo: 54
  lancamentos. Recomendado: teste em staging com 1 caso (PAGIS1/2025/00003,
  payment 50365, ja validado manualmente).

### [IMPLEMENTADO] IMP-2026-06-17-001: Automacao para baixar XML do CT-e no SSW a partir do numero da NF
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Decisao**: respondido (auto-implementado — melhoria documental)
- **Implementado**: sim (capacidade-nucleo ja existia)
- **Arquivos afetados**: `.claude/skills/operando-ssw/SKILL.md`
- **Notas**: A capacidade-NUCLEO JA EXISTE: `consultar_ctrc_101.py` (opcao 101,
  READ-ONLY via Playwright) ja aceita `--nf <numero> --baixar-xml`, baixa o ZIP,
  extrai o XML e retorna o path no campo JSON `xml` (linhas 247-348). A sugestao
  do Agent SDK desconhecia isso. O unico gap REAL era a ENTREGA — o XML fica em
  `/tmp` sem link de download. Adicionada nota no Decision Tree da `operando-ssw`
  conectando o `xml_path`/`dacte` a `exportando-arquivos`
  (`exportar.py --formato texto --arquivo <xml_path> --nome cte_nf_<numero>`,
  que aceita `.xml` com mimetype proprio) e instruindo a responder com
  `arquivo.url_completa`. Sem codigo novo necessario.

## Resumo
- Total avaliadas: 4
- Implementadas automaticamente: 2 (IMP-2026-06-16-002 skill_bug; IMP-2026-06-17-001 melhoria documental sobre capacidade ja existente)
- Rejeitadas: 0
- Propostas para revisao humana: 2 (IMP-2026-06-16-003 toca routes.py + skill WRITE nova; IMP-2026-06-16-004 operacoes financeiras destrutivas Odoo PROD)
- Persistencia DB: 4/4 OK (HTTP 200, ids 193-196)
- Testes: 72 passed em `tests/odoo/services/test_stock_picking_service.py` (70 baseline + 2 novos)
