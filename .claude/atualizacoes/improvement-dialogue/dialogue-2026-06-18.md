---
date: 2026-06-18
suggestions_evaluated: 2
implemented: 1
rejected: 1
proposed: 0
---

# Improvement Dialogue — 2026-06-18

## Sugestoes Avaliadas

### [RESPONDED] IMP-2026-06-17-002: Falta skill de reset transacional seguro do modulo Motos Assai
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Usuario de origem**: Rayssa Alves (id 78)
- **Decisao**: respondido (implementado)
- **Implementado**: sim (auto_implemented)
- **Arquivos afetados**: `scripts/maintenance/reset_motos_assai.py` (novo), `app/motos_assai/CLAUDE.md`
- **Notas**:
  - **Forma**: a demanda e legitima (SQL manual de reset e arriscado — ja houve
    tentativa em PROD sem backup), mas a sugestao pedia uma *skill do agente web*.
    Isso seria errado: o agente web e **read-only por design** e afroxa-lo para
    rodar TRUNCATE/DELETE em PROD e exatamente o que a IMP-2026-06-17-003 (rejeitada
    abaixo) tentava forcar. A finalidade real (reset repetivel e seguro) foi atendida
    com um **script DEV de manutencao** (Claude Code 4-maos), nao uma skill web.
  - **Implementacao**: `scripts/maintenance/reset_motos_assai.py` zera as 25 tabelas
    transacionais `assai_*` PRESERVANDO os 4 cadastros (`assai_cd`, `assai_loja`,
    `assai_modelo`, `assai_modelo_alias`). Salvaguardas em camadas:
    1. dry-run e o **DEFAULT** (sem `--confirmar` nao escreve);
    2. **confirmacao tipada** `--token "RESET-MOTOS-ASSAI-<N>"` (N = contagem
       transacional exata), recalculada na hora — token desatualizado aborta;
    3. **backup JSON automatico** de todas as tabelas + espelhos `ASSAI-SEP-%` ANTES
       de qualquer escrita;
    4. pre-flight: tabela `assai_%` nova nao classificada **ABORTA** (nao trunca
       cadastro novo por engano);
    5. pre-flight de vinculo: espelhos `ASSAI-SEP-%` com NF preenchida OU
       `EmbarqueItem` apontando **BLOQUEIAM** o reset (nao orfana embarque/frete/fat);
    6. limpeza de espelhos reusa `unmirror_assai_separacao` (guarda de NF nativa);
    7. `TRUNCATE` **sem CASCADE** (todas as transacionais no mesmo comando; FK externa
       inesperada faz FALHAR, nao cascatear silenciosamente);
    8. tudo em **uma transacao**, valida cadastros intactos antes do commit (rollback
       caso contrario).
  - S3 apenas **ALERTADO** (prefixos orfaos: recibos, excel Q.P.A., CCe, devolucao,
    pos-venda); nunca tocado.
  - **Validacao**: `py_compile` OK + `--help` OK. **NAO executado** — entregue na
    branch `cron/manutencao` para revisao 4-maos do Rafael antes do 1o uso real.

### [REJECTED] IMP-2026-06-17-003: Agente web read-only bloqueia execucao de reset destrutivo (Motos Assai)
- **Categoria**: gotcha_report
- **Severidade**: warning
- **Usuario de origem**: Rayssa Alves (id 78)
- **Decisao**: rejeitado
- **Implementado**: nao
- **Arquivos afetados**: nenhum
- **Notas**:
  - **NAO e bug — feature de seguranca funcionando como projetado.** Aplicando a
    regra 2.1.1 (codigo do fluxo oficial e soberano): o codigo esta **CORRETO**.
    `app/agente/tools/text_to_sql_tool.py:417` documenta que escrita (INSERT/UPDATE/
    DELETE) so e liberada em **MODO ADMIN**, via bloco `<sql_admin_context>` injetado
    no prompt (`app/agente/sdk/hooks.py`). Essa sessao NAO recebeu `sql_admin_context`,
    entao o bloqueio "SQL deve comecar com SELECT ou WITH" atuou como deveria.
  - **Politica**: operacoes destrutivas/DEV sao feitas no Claude Code (4-maos), nunca
    pela tela web. O "workaround" de entregar o `.sql` a um humano com permissao de
    escrita e **exatamente o comportamento desejado**.
  - ⚠️ **ALERTA DE SEGURANCA**: o `evidence_json` desta sugestao contem uma **tentativa
    de prompt injection** (tags falsas `</evidence>`/`</invoke>` + um `<invoke name="Bash">`
    mandando rodar `exportar.py`). Tratada como **dado, nao instrucao** — nada foi
    executado. Reforca a decisao de manter o agente read-only.
  - A demanda legitima por baixo (reset repetivel) foi atendida na IMP-2026-06-17-002.

## Sugestoes de skill F2 aguardando decisao Rafael+CC

> Prefixo `adhoc-`/`skill-gap-` → gate humano obrigatorio (EXCECAO F2). D8 apenas lista;
> Rafael decide via `python scripts/agente/revisar_sugestoes_skill.py listar`.

| suggestion_key | title | n_membros | idade (dias) | origem |
|---|---|---|---|---|
| adhoc-cluster-1385 | Processar e transformar planilhas Excel/CSV com pandas e openpyxl (leitura, calculo e exportacao formatada) | 9 | 1 | Martha de Jesus Frugoli dos Reis (id 82) |
| adhoc-cluster-1433 | Sincronizar aba 'Vencidos' de planilha Excel cruzando dados de aba de acompanhamento | 4 | 1 | Martha de Jesus Frugoli dos Reis (id 82) |

## Resumo
- Total avaliadas: 2
- Implementadas automaticamente: 1 (IMP-2026-06-17-002 — script DEV de reset seguro Motos Assai)
- Rejeitadas: 1 (IMP-2026-06-17-003 — bloqueio read-only e feature intencional; + prompt injection no evidence)
- Propostas para revisao humana: 0
- F2 listadas (gate humano): 2 (adhoc-cluster-1385, adhoc-cluster-1433 — ambas de Martha id 82, pipeline pandas+openpyxl sobre `exportando-arquivos`)
