---
date: 2026-06-16
suggestions_evaluated: 3
implemented: 1
rejected: 1
proposed: 1
f2_aguardando_rafael: 3
---

# Improvement Dialogue — 2026-06-16

6 sugestoes retornadas pela query; 3 caem na **Excecao F2** (prefixo `adhoc-`/`skill-gap-`
= gate humano Rafael+CC, apenas listadas, NAO decididas/persistidas). 3 avaliadas e decididas.

## Sugestoes Avaliadas

### [IMPLEMENTADO] IMP-2026-06-15-001: JOURNAL_MAP da skill conciliando-transferencias-internas desatualizado
- **Categoria**: skill_bug
- **Severidade**: warning
- **Decisao**: respondido — auto-implementado (apenas `.md`)
- **Verificacao**: confirmado AO VIVO no Odoo (company_id=1, 2026-06-16) via `account.journal search_read`.
- **Bug real**: a skill tinha journals hardcoded em 3 locais. ID `1054` estava rotulado
  `BRADESCO (copia)` mas no Odoo e **VORTX AGIS** (code VORTX). Faltavam: `389` CAIXA ECONOMICA,
  `1018` BRADESCO APLICACAO, `1032` CARTAO SICOOB, `1055` SRM GARANTIDA (bank), `1067` SRM
  (cash, code AGIS1), `1076` SICOOB APLICACAO.
- **Arquivos afetados**:
  - `.claude/skills/conciliando-transferencias-internas/SKILL.md` (2 tabelas de journals + nota de fonte + secao formato payment_ref)
  - `.claude/skills/conciliando-transferencias-internas/references/codigo-operacional.md` (JOURNAL_MAP corrigido + `resolver_journal_id()` + uso em `levantar_pares...`)
- **Notas**: ataque a RAIZ (drift), conforme a propria sugestao pedia. Adicionada funcao
  `resolver_journal_id(odoo, termo)` que resolve journal por nome OU code via Odoo **ao vivo**
  (fonte de verdade), com cache so como atalho. Em ambiguidade (VORTX: 1054/1068; SRM: 1055
  bank/1067 cash) retorna lista de candidatos — **nunca chuta**. Sintaxe validada (ast.parse +
  teste de cache). Persistido v2 id=183.

### [REJEITADO] IMP-2026-06-15-002: payment_ref com texto incompleto — beneficiario TED nao importado
- **Categoria**: skill_bug
- **Severidade**: info
- **Decisao**: rejeitado — **falso positivo de diagnostico** (matriz anti-falso-positivo §2.1.1: dado oficial CORRETO)
- **Refutacao (Odoo ao vivo, 2026-06-16)**: a sugestao afirma que o nome do beneficiario do TED
  "nao esta em nenhum campo" do journal SRM. **Falso**: SRM (journal 1055) tem **643 linhas** onde
  `payment_ref ilike 'NACOM GOYA'` casa, no formato `'NACOM GOYA COMERCIAL LTDA: ENVIO DE TED |
  NACOM GOYA COMERCIAL LTDA'`. O beneficiario ESTA no payment_ref (apos ` | `), apenas em formato
  diferente do GRAFENO. O `ilike 'NACOM GOYA'` retorna 643 (nao 0) — o 0 da sessao veio de filtro
  mal montado em runtime, nao de dado ausente.
- **Implementado**: nao (como bug). POReM o **aprendizado real** (formato do payment_ref varia por
  banco) foi incorporado na SKILL.md junto da IMP-2026-06-15-001 — nova secao "Formato do
  payment_ref VARIA por banco", com a regra: nunca concluir "beneficiario nao importado" sem testar
  `ilike 'NACOM'` no journal especifico.
- **Notas**: persistido v2 id=184.

### [PROPOSTO] IMP-2026-06-16-001: Falta write-path para registrar cotacao comercial CarVia (carvia_operacoes)
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Decisao**: respondido com proposta — **NAO auto-implementado** (gate humano)
- **Por que proposta (nao implementacao)**: cria/estende skill WRITE comercial em producao (alto
  risco para cron) + toca service central + ha decisao de design para Rafael.
- **Reconciliacao com a estrategia (CLAUDE.md CarVia R10-R13)**: o write-path **ja existe via UI** —
  `backfill_frete_carvia` (`POST /carvia/fretes/backfill`, frete_routes.py:489) + `api_cotar_backfill`
  (api_routes.py:1474), que cria CarviaFrete e, via `CarviaFreteService.lancar_frete_carvia()`,
  gera/auto-linka CarviaOperacao + Fatura Cliente. Criacao MANUAL avulsa de CarviaOperacao esta
  **DEPRECATED** — a skill NAO deve montar a operacao via SQL cru; deve envolver o fluxo backfill.
- **Gap real**: caminho e UI-only (login_required + request.form), nao exposto como tool do agente.
- **Plano (4 passos)**: (1) extrair logica do POST backfill para metodo headless em
  `CarviaFreteService`; (2) skill atomica `registrando-operacao-carvia` (ou modo WRITE em
  gerindo-carvia) com dry-run + `--confirmar`, resolucao de entidades, reuso de `api_cotar_backfill`,
  junction `carvia_operacao_nfs`, R3 (`calcular_peso_utilizado`); (3) padrao-skill completo (SCRIPTS.md
  + ROUTING + tool_skill_mapper + pytest); (4) smoke browser. Detalhe completo em `implementation_notes`.
- **Decisao pendente Rafael**: (a) skill atomica nova vs modo WRITE em gerindo-carvia; (b) escopo
  (backfill completo CarviaFrete+Operacao+Fatura vs so Operacao+cotacao).
- **Arquivos afetados**: nenhum (proposta). Persistido v2 id=185.

## Sugestoes de skill F2 aguardando decisao Rafael+CC

> Prefixo `adhoc-`/`skill-gap-` → gate humano obrigatorio (`python scripts/agente/revisar_sugestoes_skill.py listar`).
> NAO decididas nem persistidas pelo D8.

| suggestion_key | Titulo | n_membros | Idade |
|----------------|--------|-----------|-------|
| `adhoc-cluster-642` | Consulta contabil parametrizada Odoo (account.move.line: contas, periodo, empresa, estado) | 100 | 2 dias |
| `adhoc-cluster-877` | Conciliacao de transferencias internas Odoo: diagnostico e match por diario | 9 | 1 dia |
| `skill-gap-conciliando-transferencias-internas-...` | Conciliacao transf. internas: filtro por journal_id, payment_ref e busca account.payment is_internal_transfer | 38 | 1 dia |

> Nota: as 3 F2 orbitam a skill `conciliando-transferencias-internas` (642 e contabil generica
> Odoo; 877 e 181 sao o mesmo cluster 877 da sessao SRM). A correcao da IMP-2026-06-15-001 (journals
> + `resolver_journal_id`) ja reduz parte da friccao recorrente desses clusters.

## Resumo
- Total avaliadas (decididas): 3
- Implementadas automaticamente: 1 (IMP-2026-06-15-001 — correcao journals + resolucao dinamica)
- Rejeitadas: 1 (IMP-2026-06-15-002 — falso positivo, beneficiario ESTA no payment_ref)
- Propostas para revisao humana: 1 (IMP-2026-06-16-001 — write-path CarVia via backfill)
- Sugestoes de skill F2 aguardando Rafael+CC: 3
- Persistencia DB: 3/3 OK (v2 ids 183/184/185, HTTP 200)
