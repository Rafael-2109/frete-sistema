---
date: 2026-06-25
suggestions_evaluated: 6
implemented: 0
rejected: 1
proposed: 1
reconciled: 4
f2_listed: 2
---

# Improvement Dialogue — 2026-06-25

Cron D8 normal. Backlog do PASSO 1 = 2 sugestoes avaliaveis (`warning`) + 2 F2 `adhoc-`
(gate humano). PASSO 1.5 reconciliou **4 propostas** antigas que viraram codigo depois do
registro v2. **0 auto-implementadas** — as 2 avaliaveis sao 1 falso positivo e 1 nao-bug
(toca `routes.py`, so proposta). v2 ids 301-302, v3 ids 303-306 (HTTP ok).

## Sugestoes Avaliadas

### [REJEITADA] IMP-2026-06-24-005: button_draft via XML-RPC falha com None — usar JSON-RPC
- **Categoria**: gotcha_report
- **Severidade**: warning
- **Usuario de origem**: TAMIRIS SALLES CORDEIRO (id 44)
- **Decisao**: rejeitado (falso positivo)
- **Implementado**: nao
- **Arquivos afetados**: nenhum
- **Notas**: Matriz anti-falso-positivo 2.1.1 — **codigo do fluxo oficial CORRETO + proveniencia
  ad-hoc PRESENTE**. O fluxo oficial da skill `reclassificando-amls-odoo` (`reclassificar_amls.py`
  -> `ReclassificacaoService.executar` -> `app/odoo/estoque/scripts/reclassificacao.py:239,244`
  usa `self.odoo.execute_kw` p/ `button_draft` e `action_post`) JA trata `cannot marshal None`
  como SUCESSO: `OdooConnection.execute_kw` captura o Fault via `is_cannot_marshal_none()` e
  retorna `None` (`app/odoo/utils/connection.py:24-30,265-269`), **desde o commit `305ccb8dd`
  (2026-01-26)** — meses antes da sessao de 24/06. Logo XML-RPC puro funciona; migrar p/ JSON-RPC
  e desnecessario e PIOR (exigiria autenticacao por senha real em vez de `api_key` = regressao de
  seguranca). O gotcha JA esta documentado: `GOTCHAS.md` l.169 (button_validate), **l.420-440
  (ciclo button_draft->write->action_post via `connection.execute_kw`)**, l.539 (tabela de erros
  -> `is_cannot_marshal_none`); + `recebimento-fisico-odoo/SKILL.md:18,36` +
  `executando-odoo-financeiro/erros-comuns.md:70`. A falha "53/53 via XML-RPC" descrita veio de
  **scripts ad-hoc com `xmlrpc.client.ServerProxy` CRU** na sessao `bf1aff40` (`agent_adhoc_script`
  ids 3725-3749, `skill_relacionada=rastreando-odoo`), que nao passam pelo try/except do
  `execute_kw`. Friccao real = roteamento: o agente caiu em ad-hoc em vez de usar a skill
  `reclassificando-amls-odoo` (que ja resolve). Nenhuma alteracao de codigo.

### [PROPOSTA] IMP-2026-06-24-006: valor_cotado congelado no UI + agente sem write para corrigir
- **Categoria**: skill_bug
- **Severidade**: warning
- **Usuario de origem**: TALITA DE LE LIMA (id 17)
- **Decisao**: respondido (proposta — toca `routes.py`, opcao C)
- **Implementado**: nao (plano em implementation_notes)
- **Arquivos afetados**: nenhum (proposta)
- **Notas**: Avaliado em 2 partes.
  - **(a) pedagio "ceil vs proporcional" NAO e bug** — e configuravel por transportadora via
    `Transportadora.pedagio_por_fracao` (Boolean default True, `app/transportadoras/models.py:62`),
    respeitado em `calculadora_frete.py:150-153` (`_calcular_pedagio_v2 por_fracao`) e
    `fretes/routes.py:1368`. Pedagio por fracao de 100 kg (arredonda p/ cima) e regra de negocio
    padrao; p/ a transportadora Campo Grande/Alemar cobrar proporcional basta `pedagio_por_fracao=False`
    no cadastro.
  - **(b) valor_cotado "congelado" e DESIGN INTENCIONAL** — snapshot da tabela no lancamento
    (`routes.py:789`), baseline da tolerancia R$5 (R6 do CLAUDE.md de fretes). O ajuste do valor
    real JA EXISTE e e editavel na MESMA tela `/fretes/<id>/editar` via campo `valor_considerado`
    (`routes.py:895-897`; inicia = cotado em `:807`). Sobrescrever `valor_cotado` destruiria o
    baseline de auditoria de divergencia.
  - **Proposta de conveniencia (gap legitimo)**: botao "Recalcular cotacao (cadastro atual)" em
    `/fretes/<id>/editar` -> novo endpoint POST `/fretes/<id>/recalcular-cotacao` que chama a MESMA
    `calcular_valor_frete_pela_tabela` com a tabela vigente e grava em `valor_considerado` (NUNCA em
    `valor_cotado`), re-aplicando a regra de aprovacao (diff>R$5 -> EM_TRATATIVA) ja em
    `editar_frete:907-926`; bloquear status final (PAGO/LANCADO_ODOO/CANCELADO). Core em `routes.py`
    => apenas proposta.

## Reconciliacao de propostas implementadas (PASSO 1.5)

4 propostas antigas (`auto_implemented=false`) viraram codigo depois do registro v2 — gravadas como
**v3 `auto_implemented=true`**:

| suggestion_key | Proposta em | Commit que implementou | v3 |
|----------------|-------------|------------------------|----|
| IMP-2026-06-10-005 (CarVia no Lancamento Freteiros) | 2026-06-12 | `bf4f61bc6` feat(fretes+carvia) lancamento unificado (2026-06-12) | id 303 |
| IMP-2026-06-16-004 (conciliando-transferencias: substituir perna em journal errado) | 2026-06-17 | `7c13865f0` feat Situacao 3 (2026-06-17) | id 304 |
| IMP-2026-06-19-007 (recuperar upload por ID/prefixo S3 apos rotacao) | 2026-06-20 | serie S3 (`7744aa372`/`f4c8002be`/`d81a3e89d`/`630f7acf4`/`42f543425`/`b1065e30c`/`802c95f56`/`de9c72ea4`) (2026-06-20) | id 305 |
| IMP-2026-06-19-010 (3 skills WRITE faturamento planilha Q.P.A.) | 2026-06-20 | `37ca214cc` modo devolucao-nfd (2026-06-20) — fecha a 3a capacidade; skill `corrigindo-dados-assai` ja cobre as 3 | id 306 |

> Propostas que so tinham hit de commit `improvement(D8)...` (relatorio que cita a key, nao
> implementa) permanecem pendentes: IMP-2026-06-01-001, -06-08-002, -06-16-001, -06-16-003.

## Sugestoes de skill F2 aguardando decisao Rafael+CC

> Prefixo `adhoc-`/`skill-gap-` = gate humano (EXCECAO F2). NAO avaliadas nem persistidas pelo D8.
> Decisao: `python scripts/agente/revisar_sugestoes_skill.py listar`.

- **adhoc-cluster-3734** — "Configurar conta contabil de destino em operacoes CD/OUT no Odoo via
  XML-RPC" (6 membros · TAMIRIS id 44 · ~1 dia). Padrao WRITE de conta de destino em tipo de
  operacao de estoque (conta 3201000001, op CD/OUT) — mesma sessao `bf1aff40` da IMP-005.
- **adhoc-cluster-966** — "Consultar cadastro e metricas de clientes/parceiros no Odoo
  (res.partner)" (178 membros · Martha id 82, VANDERLEIA id 48 · ~1 dia). Consulta/filtragem de
  `res.partner` (CNPJ, nome, active, customer_rank) + agrupamento por vendedor via `sale.order` —
  reconhecidamente fora do escopo de `rastreando-odoo`.

## Resumo
- Total avaliadas: 6 (2 do backlog + 4 reconciliadas)
- Implementadas automaticamente: 0
- Rejeitadas: 1 (IMP-2026-06-24-005 — falso positivo)
- Propostas para revisao humana: 1 (IMP-2026-06-24-006)
- Reconciliadas (v3 auto_implemented=true): 4
- F2 listadas (gate humano): 2
- Persistencia: v2 ids 301-302, v3 ids 303-306 (HTTP ok). Branch `cron/manutencao`, sem push.
