---
date: 2026-05-29
suggestions_evaluated: 1
implemented: 0
rejected: 1
proposed: 0
---

# Improvement Dialogue — 2026-05-29

## Sugestoes Avaliadas

### [REJECTED] IMP-2026-05-28-001: Peso cubado nunca calculado no fluxo de cotacao CarVia
- **Categoria**: skill_suggestion (na pratica, melhoria de codigo do modulo `app/carvia/`)
- **Severidade**: warning
- **Decisao**: rejeitado
- **Implementado**: nao
- **Arquivos afetados**: nenhum
- **Persistencia**: v2 gravada no banco (id=123, HTTP 200)
- **Notas**: A sugestao propoe "implementar preenchimento de dimensao_c/l/a e calcular `peso_cubado = (C x L x A) / 300` ao criar cotacao de motos". A proposta foi rejeitada por tres motivos verificados contra o codebase e o banco de producao (Render):

  **1. O calculo de peso cubado JA EXISTE** (a premissa "nunca calculado" e incorreta):
  - CARGA_GERAL: `app/carvia/services/pricing/cotacao_v2_service.py:96-98` → `peso_cubado = C x L x A x 300` (dimensoes em metros, fator 300 kg/m3).
  - CARGA_GERAL (edicao): `app/carvia/routes/cotacao_v2_routes.py:1678-1682` e `1924-1928` (mesma formula `x 300`).
  - MOTO: `cotacao_v2_service.py:143-148` → `peso_cubado_unitario = C x L x A x cubagem / 1.000.000` (dimensoes do **modelo de moto** em cm).
  - MOTO (agregado): `cotacao_v2_routes.py:2241-2242` e `2372-2373` → `cotacao.peso_cubado = SUM(CarviaCotacaoMoto.peso_cubado_total)`.

  **2. A formula proposta esta INCORRETA**: o sistema usa `x 300` (CARGA_GERAL) ou `x cubagem / 1.000.000` (MOTO). A formula `(C x L x A) / 300` da sugestao esta invertida e produziria valores com ordem de grandeza errada — implementa-la introduziria um bug de precificacao.

  **3. Para MOTO, `dimensao_c/l/a` da cotacao sao NULL POR DESIGN**: as dimensoes fisicas vem do `CarviaModeloMoto` (comprimento/largura/altura), nao da cotacao. A evidencia da sugestao (COT-97, cliente MARCELA - MOTOCHEFE) e uma cotacao MOTO. Confirmado no banco: das 132 cotacoes, **0 tem `dimensao_c > 0`** (86 MOTO + 46 CARGA_GERAL); os **32/32 modelos de moto tem dimensoes + cubagem** cadastradas; e a propria **COT-97 tem peso cubado correto nas motos**: BIG TRI 952,698 kg + JOY SUPER 941,460 kg = **1.894,158 kg**.

  **Causa-raiz REAL do sintoma observado** (`embarque_item 13362.peso_cubado = NULL, volumes = 0`):
  - **Matching de nome de modelo** por substring simples em `app/carvia/services/documentos/embarque_carvia_service.py:633-643` (`nome in mod or mod in nome`). Na NF 37939 os veiculos chegam como `"TRI MOTO ELETRICA BIG-TRI"` (x5) e `"SUPER SCOOTER ELETRICA JOY SUPER"` (x10); a cotacao cadastra `"BIG TRI"` e `"JOY SUPER"`. `"BIG TRI"` (espaco) **nao** e substring de `"BIG-TRI"` (hifen) → o cubado das 5 BIG TRI nao casa.
  - **Cotacao manual**: COT-97 e `cotacao_manual=True`; `definir_valor_manual` (`cotacao_v2_service.py:469-516`) e `adicionar_moto` (`106-174`) nao propagam `cotacao.peso_cubado` (apenas a property `peso_total_motos` reflete em tempo real — `models/cotacao.py:188-196`).

  **Recomendacao para revisao humana** (NAO implementada — toca `routes.py`, arquivo restrito ao D8, e logica de matching com efeitos em embarques/precificacao reais):
  - (a) Normalizar o nome do modelo no match de `calcular_cubado_por_modelos` (tratar hifen/espaco/acentos, ou word-boundary tolerante a hifen).
  - (b) Garantir propagacao de `cotacao.peso_cubado` nos caminhos manuais, ou usar consistentemente a property `peso_total_motos` ao provisionar o `embarque_item`.

## Resumo
- Total avaliadas: 1
- Implementadas automaticamente: 0
- Rejeitadas: 1
- Propostas para revisao humana: 0 (recomendacao de causa-raiz registrada nas notas; correcao real fica fora do escopo auto-implementavel do D8 por tocar `routes.py` + logica sensivel)
