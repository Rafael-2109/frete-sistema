---
date: 2026-05-14
suggestions_evaluated: 4
implemented: 4
rejected: 0
proposed: 0
---

# Improvement Dialogue — 2026-05-14

## Sugestoes Avaliadas

### [RESPONDED] IMP-2026-05-14-001: action_update_taxes zera impostos quando fiscal_position mapeia para vazio
- **Categoria**: gotcha_report
- **Severidade**: critical
- **Sessao**: 4722693c-3dd2-4bb7-b03c-4724af47a797
- **Decisao**: respondido + implementado
- **Implementado**: sim
- **Arquivos afetados**:
  - `.claude/references/odoo/GOTCHAS.md` (nova linha na tabela "Comportamentos Inesperados" + secao detalhada "Recalcular Impostos em sale.order (BR)")
- **Notas**: O metodo nativo Odoo `action_update_taxes` reaplica `fiscal_position.map_tax()` sobre `account.tax`. Quando a fiscal_position mapeia impostos para vazio (caso de "SAÍDA - TRANSFERÊNCIA ENTRE FILIAIS" ID 49), todas as linhas perdem `tax_id` → `amount_tax=0`. Sem rollback automatico. Gotcha agora documentado com o metodo CORRETO (`onchange_l10n_br_calcular_imposto`, usado pelo worker `app/pedidos/workers/impostos_jobs.py`). Verificacao previa: GOTCHAS.md ja citava `onchange_l10n_br_calcular_imposto` em outros contextos (account.move pos-write, NF-e Transfer) mas NAO mencionava o anti-padrao especifico em sale.order com fiscal_position vazia.

### [RESPONDED] IMP-2026-05-14-002: Agente deve consultar codigo-fonte do worker de impostos antes de executar recalculo no Odoo
- **Categoria**: instruction_request
- **Severidade**: critical
- **Sessao**: 4722693c-3dd2-4bb7-b03c-4724af47a797
- **Decisao**: respondido + implementado (combinado com #001)
- **Implementado**: sim
- **Arquivos afetados**:
  - `.claude/references/odoo/GOTCHAS.md` (referencia ao worker `app/pedidos/workers/impostos_jobs.py` como fonte canonica)
  - `app/agente/prompts/system_prompt.md` (regra R3.2 incorpora a instrucao "usar onchange_l10n_br_calcular_imposto, nao action_update_taxes")
- **Notas**: Sugestao complementar a #001. Em vez de criar regra abstrata "leia o codigo-fonte antes de executar", a abordagem mais robusta foi documentar o anti-padrao + metodo correto em GOTCHAS.md (consultado por todos os subagentes Odoo) e incorporar a referencia direta no system_prompt.md (R3.2). Worker correto e `app/pedidos/workers/impostos_jobs.py:139-145` usando `onchange_l10n_br_calcular_imposto` via XML-RPC com `cannot marshal None` tratado como sucesso.

### [RESPONDED] IMP-2026-05-14-003: Agente deve bloquear operacoes de massa em SOs com NF-e posted sem confirmacao explicita dos 3 riscos
- **Categoria**: instruction_request
- **Severidade**: warning
- **Sessao**: 4722693c-3dd2-4bb7-b03c-4724af47a797
- **Decisao**: respondido + implementado
- **Implementado**: sim
- **Arquivos afetados**:
  - `app/agente/prompts/system_prompt.md` (nova sub-regra **R3.2** dentro de R3 "Confirmacao Obrigatoria", analoga a R3.1)
- **Notas**: Sub-regra R3.2 modela explicitamente o caso "picking done + account.move posted" exigindo confirmacao TIPADA SEPARADA para cada um dos 3 riscos:
  1. "Confirmo NF imutavel" (NF-e SEFAZ irreversivel)
  2. "Confirmo backlog complementar" (NF complementar pendente)
  3. "Confirmo novo picking" (delta de quantidade gera novo picking)
  Confirmacao agregada ("Sim para as 3") explicitamente PROIBIDA. Inclui tambem proibicao de `action_update_taxes` na mesma regra (cross-ref com R3.2 e GOTCHAS.md). Section `<why>` ampliada com motivacao fiscal (NF-e irreversivel, picking fantasma = sonegacao, impostos zerados = contestacao SEFAZ).

### [RESPONDED] IMP-2026-05-14-004: gerando-baseline-conciliacao — ordenacao de meses lexicografica em vez de cronologica na Aba 1
- **Categoria**: skill_bug
- **Severidade**: warning
- **Sessao**: eb3477d4-cd2d-4f1b-ae88-c39e8373b5a1
- **Decisao**: respondido + implementado
- **Implementado**: sim
- **Arquivos afetados**:
  - `.claude/skills/gerando-baseline-conciliacao/scripts/gerar_baseline.py` (3 mudancas: nova funcao `_mes_ano_sort_key` + correcao na linha de sort da Aba 1 + correcao na linha de sort da Aba 4 Resumo)
- **Notas**: Bug confirmado no codigo. A funcao `_fmt_mes_ano` retorna string `MM/YYYY` ("04/2025", "01/2026") — `sorted(...)` aplicava ordenacao lexicografica que inverte meses entre anos ("01/2026" < "04/2025" mesmo sendo posterior cronologicamente). Fix com helper `_mes_ano_sort_key(mes_ano)` que parseia para tupla `(YYYY, MM)`. Aplicado em DOIS pontos: Aba 1 (`Pendentes Mes x Journal`, linha 292) e Aba 4 (`Resumo`, linha 401 — mesma raiz, nao mencionada na sugestao mas detectada na revisao). Heuristica documentada em `/memories/empresa/heuristicas/financeiro/baseline-de-extratos-formato-fixo.xml` item 6 do `<do>` agora honrada no codigo.

## Resumo
- Total avaliadas: 4
- Implementadas automaticamente: 4
- Rejeitadas: 0
- Propostas para revisao humana: 0

## Cross-Cutting

As 3 sugestoes da mesma sessao (4722693c) — IMP-001/002/003 — descrevem 3 facetas
de um unico incidente operacional: o agente executou `action_update_taxes` em SO
ja faturado, zerando impostos. As 3 melhorias foram implementadas em UMA
correcao coerente:

1. **Documentar o anti-padrao** (GOTCHAS.md tabela + secao detalhada)
2. **Apontar o metodo correto** (referencia ao worker `impostos_jobs.py`)
3. **Bloquear no system_prompt** (R3.2 com confirmacao TIPADA por risco)

A 4a sugestao (IMP-004) e independente: bug isolado no script da skill
gerando-baseline-conciliacao. Fix aplicado em 3 pontos do mesmo arquivo.

## Verificacao tecnica

- `gerar_baseline.py`: diagnostics de Pyright nas linhas 305/326/341 sao pre-existentes (openpyxl `ws1[total_row]` indexing). Nao introduzidos por esta mudanca.
- GOTCHAS.md: gotcha adicionado segue padrao da tabela existente (Comportamento | Contexto | Solucao) + secao detalhada com codigo CERTO/ERRADO comparado.
- system_prompt.md R3.2: padrao identico a R3.1 (texto + ATENCAO + opcoes nomeadas + criterio de aceite "responda X").
