---
date: 2026-06-19
suggestions_evaluated: 5
implemented: 2
rejected: 1
proposed: 2
---

# Improvement Dialogue — 2026-06-19

Backlog: 5 sugestões `IMP-*` avaliáveis (todas do módulo **Motos Assaí**, todas
de **Rayssa Alves (id 78)**) + 4 sob gate F2 (`adhoc-`/`skill-gap-`, apenas
listadas). As 5 `IMP-*` são fortemente inter-relacionadas: derivam de **uma carga
histórica one-shot de 1.394 chassis** (sessões `92516689…` e `17b68633…`) que a
Rayssa subiu via scripts ad-hoc.

**Veredito**: 1 mudança atômica auto-implementada (destrava o gargalo central
citado em 2 sugestões), 2 propostas (camada de model / cruza routes+forms),
1 rejeição (over-engineering para one-shot).

> **Nota de método (feature-dev)**: a Discovery do feature-dev foi feita inline e
> por completo (service + 25 callers + model + teste + doc + REGRAS_TIMEZONE).
> Para uma mudança cirúrgica de **um parâmetro opcional retrocompatível**, spawnar
> o pipeline de 3 subagentes seria desproporcional; segui o método (explore →
> design → implement → review) manualmente, com teste de regressão.

## Sugestões Avaliadas

### [RESPONDED · implementado] IMP-2026-06-18-005: carga em lote de chassis históricos (backfill ESTOQUE)
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Usuário de origem**: Rayssa Alves (id 78)
- **Decisão**: respondido
- **Implementado**: sim (parcial — o gargalo estrutural)
- **Arquivos afetados**: `app/motos_assai/services/moto_evento_service.py`, `tests/motos_assai/test_moto_evento_service.py`, `app/motos_assai/CLAUDE.md`
- **Notas**: o item destravante (item 3 da sugestão) — `emitir_evento` não aceitar
  data retroativa, forçando instanciar `AssaiMotoEvento` na mão e furar a abstração
  do módulo — foi corrigido: `emitir_evento(..., ocorrido_em: Optional[datetime]=None)`.
  Quando fornecido (Brasil naive, conforme REGRAS_TIMEZONE), grava a data retroativa;
  quando omitido mantém o default `agora_brasil_naive`. 100% retrocompatível (os ~25
  callers usam keyword args e não passam `ocorrido_em`). Implementação defensiva:
  `ocorrido_em` só é injetado no construtor quando não-None (passar None anularia o
  default e violaria NOT NULL). +2 testes (suite 7 passed). **NÃO** implementei a
  tela/rota de importação completa: backfill one-shot já concluído, sem sinal de
  recorrência → over-engineering. Caminho recomendado para cargas futuras: script
  utilitário reaproveitando `emitir_evento(ocorrido_em=…)` + `modelo_resolver` +
  `chassi_validator` (todos já existem).

### [RESPONDED · implementado] IMP-2026-06-19-001: carga retroativa de estado em massa (planilha → eventos)
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Usuário de origem**: Rayssa Alves (id 78)
- **Decisão**: respondido
- **Implementado**: sim (item 2; itens 1 e 3 propostos/rejeitados)
- **Arquivos afetados**: `app/motos_assai/services/moto_evento_service.py`, `tests/motos_assai/test_moto_evento_service.py`, `app/motos_assai/CLAUDE.md`
- **Notas**: dos 3 itens —
  - **(2) `ocorrido_em` custom — FEITO**: mesma mudança de IMP-2026-06-18-005.
    Preserva a cronologia real numa carga de histórico (datas abr–jun/2026 não viram
    "hoje").
  - **(3) STATUS DEMONSTRACAO — PROPOSTA** (camada de model, gate humano): adicionar
    `EVENTO_DEMONSTRACAO='DEMONSTRACAO'` em `models/moto.py`, incluir em
    `EVENTOS_VALIDOS` e classificar em `EVENTOS_FORA_ESTOQUE` (não conta como estoque
    disponível). Risco baixo (coluna `tipo` é String(40), sem schema change), mas é
    model → não auto-implementado. Caso real: 3 motos em DEMONSTRAÇÃO.
  - **(1) FATURADA retroativa sem lastro fiscal — REJEITADO**: criar FATURADA órfã
    (sem `assai_nf_qpa`) viola a invariante de que FATURADA nasce do import de NF
    Q.P.A. com match BATEU. Downstream (`resumo_service`, `devolucao_service` que
    espera `nf_qpa_origem_id`, `pos_venda`, `cancelamento_nf`) assume NF vinculada →
    inconsistência silenciosa. Caminho correto: importar as NFs de fato (fluxo oficial).

### [RESPONDED · proposta] IMP-2026-06-18-002: cancelamento por loja em pedido de venda
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Usuário de origem**: Rayssa Alves (id 78)
- **Decisão**: respondido (proposta)
- **Implementado**: não (exige migration + model + service)
- **Arquivos afetados**: — (plano)
- **Notas**: feature de produto legítima (cada loja Sendas é independente: CNPJ/NF/
  entrega próprios; hoje o único workaround é DELETE da loja, que perde auditoria).
  Plano: (1) migration `ADD COLUMN status` em `assai_pedido_venda_loja` (par DDL+Python
  + regenerar schema JSON); (2) coluna no model `AssaiPedidoVendaLoja` reusando
  `PEDIDO_STATUS_*`; (3) service `cancelar_pedido_assai` com granularidade por loja +
  guard de dependências a jusante + idempotência; (4) `pedido_status_service`/
  `resumo_service` ignoram lojas CANCELADAS no recálculo e totais; (5) testes. Sob
  gate humano por tocar camada de model.

### [RESPONDED · proposta] IMP-2026-06-19-003: observação por chassi no evento FATURADA
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Usuário de origem**: Rayssa Alves (id 78)
- **Decisão**: respondido (proposta)
- **Implementado**: não (cruza routes/forms; decisão de produto)
- **Arquivos afetados**: — (plano)
- **Notas**: o evento FATURADA já tem coluna `observacao` (Text). Duas opções: (A)
  `importar_nf_qpa` recebe mapa opcional `chassi→observacao` (planilha auxiliar no
  upload) e grava em `observacao`/`dados_extras.observacao_defeito` — pequeno no
  `nf_qpa_adapter` mas exige alterar `UploadNfQpaForm`/route; (B) usar a entidade já
  existente `assai_pos_venda_ocorrencia` (defeito é semanticamente pós-venda), com
  nova categoria FÁBRICA/DEFEITO + ajuste de filtros das telas. Recomendação: A para
  a carga histórica imediata (143 motos com defeito conhecido); avaliar B se virar
  fluxo recorrente. Sob gate humano por cruzar routes/forms.

### [REJECTED] IMP-2026-06-19-002: backfill de recibo Motochefe conferido em massa
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Usuário de origem**: Rayssa Alves (id 78)
- **Decisão**: rejeitado
- **Implementado**: não
- **Arquivos afetados**: —
- **Notas**: operação one-shot de migração histórica (1.393 chassis), já resolvida via
  script INSERT idempotente (prefixo `MA-HIST-`) com dry-run + confirmação. A própria
  sugestão confirma que criar compra/recibo **não reflete na Nacom** (só o faturamento
  reflete) — logo o objetivo (destravar o faturamento das 105 NFs) já é alcançável sem
  feature nova. O atrito recorrente real (registrar evento com data retroativa sem furar
  a abstração) foi resolvido em IMP-2026-06-18-005. Criar skill/feature permanente para
  algo que aconteceu uma vez = over-engineering (output style §6). Se a operação virar
  recorrente (onboarding de novos CDs), transformar o script validado num utilitário em
  `scripts/migrations/` — não em UI.

## Sugestões de skill F2 aguardando decisão Rafael+CC

> Gate humano obrigatório (prefixo `adhoc-`/`skill-gap-`). D8 apenas lista
> (`scripts/agente/revisar_sugestoes_skill.py listar`). Nenhuma decisão/status escrito.

| suggestion_key | título | origem | n_membros | idade |
|---|---|---|---|---|
| `adhoc-cluster-1385` | Processar/transformar planilhas Excel/CSV (pandas + openpyxl) | Martha de Jesus Frugoli dos Reis (id 82) | 9 | 2d |
| `adhoc-cluster-1433` | Sincronizar aba 'Vencidos' cruzando aba de acompanhamento | Martha de Jesus Frugoli dos Reis (id 82) | 4 | 2d |
| `adhoc-cluster-1070` | Gerar embarque + lançar fretes automaticamente | TALITA DE LE LIMA (id 17) | 23 | 1d |
| `skill-gap-lendo-arquivos-…` | Análise exploratória + validação de integridade de planilhas | Rayssa Alves (id 78) | 2 | <1d |

- **1385 / 1433** já foram **estudadas a fundo em 18/06** (ver `dialogue-2026-06-18.md`):
  re-enquadradas de "skill de Excel" para **gaps de domínio** (parser extrato Grafeno +
  gestor de carteira no relatório de vencidos), com decisão preliminar de **NÃO criar
  skill de Excel**. Permanecem em `version=1` na fila F2 aguardando decisão formal do Rafael.
- **1070 (Talita)** e **skill-gap-lendo-arquivos (Rayssa)** são novas na fila F2.

## Resumo
- Total avaliadas: 5
- Implementadas automaticamente: 2 (IMP-2026-06-18-005, IMP-2026-06-19-001 — ambas pela mesma mudança atômica `ocorrido_em`)
- Rejeitadas: 1 (IMP-2026-06-19-002)
- Propostas para revisão humana: 2 (IMP-2026-06-18-002, IMP-2026-06-19-003)
- F2 listadas (sem decisão): 4
- Persistência DB: 5/5 OK (v2 ids 216–220, HTTP 200)
- Testes: `tests/motos_assai/test_moto_evento_service.py` 7 passed

---

## Revisão 4-mãos (2026-06-19) — reconciliação pós-execução (Rafael + Claude Code)

O D8 lê o backlog do banco e, para as F2, reproduziu o enquadramento do estudo de
18/06 sem reconciliar com o que foi commitado nas horas seguintes (rodou 19/06 11:13).
Correções desta sessão:

- **F2 financeiro `adhoc-cluster-1385` / `adhoc-cluster-1433` (Martha id 82) — JÁ ATENDIDAS, agora FECHADAS.**
  Quando o D8 rodou, a demanda já estava na `main` desde 18/06: `gerando-controle-recebiveis`
  (commit `ab7cd1887`, aba "Vencidos por gestor" via `equipe_vendas`) + `baixando-credores-lote-odoo`
  (`9de4dcdec`) + fix do validador SQL que bloqueava `vendedor`/`equipe_vendas` (`0cbbbd9f6`).
  As entradas seguiam `version=1` porque **criar a skill não fecha a entrada F2** no banco —
  causa-raiz da relistagem. Fechadas via `revisar_sugestoes_skill.py fechar` (v2 ids 221/222,
  `status=closed`, v1 propagada). Gap remanescente: parser do extrato Grafeno
  (`app/financeiro/parsers/dispatcher.py:45` "Futuro") — item de domínio próprio, em aberto.

- **IMP-2026-06-19-003 (observação de defeito no FATURADA) — opção A reavaliada.**
  O objetivo (registrar defeito por chassi vendido) já é atendível **sem código novo** via
  `pos_venda_service.criar_ocorrencia` (`app/motos_assai/services/pos_venda_service.py:236`;
  valida chassi vendido em `assai_nf_qpa_item`, aceita descrição + anexos S3) — a "opção B"
  que o próprio relatório citou. A opção A (mapa `chassi→observacao` no `importar_nf_qpa` +
  `UploadNfQpaForm`) é desnecessária. Decisão final (fechar como resolvível vs rejeitar)
  pendente da análise do contexto da sessão de origem.

- **`ocorrido_em` (IMP-005 / IMP-001) integrado na `main`** nesta sessão (antes vivia só na
  worktree `cron/manutencao`). Testes revalidados na `main`.
