---
date: 2026-06-24
suggestions_evaluated: 7
implemented: 5
rejected: 0
proposed: 2
---

# Improvement Dialogue — 2026-06-24

**Reconciliacao DEV (sessao 4-maos, NAO cron).** As correcoes foram implementadas
direto na sessao de desenvolvimento (commits abaixo) e aqui o backlog dos IMP da
carga historica Q.P.A. (Rayssa Alves id 78) e atualizado para refletir o que foi
de fato resolvido. 7 IMP atualizados em PROD (v2 ids 261-262, 277-281, HTTP 200).
IMP-23-006 (diario inter-company) e IMP-23-007 (skill add-item-embarque) NAO sao
Q.P.A. — seguem `proposed` para o D8 normal.

Commits: `17d5c4b04` (data loss), `181aba751` (guards de import + backfill
split-brain), `bd8fe19b2` (guard CCe correcao-de-pedido).

## Implementados (5)

### [IMPLEMENTADO] IMP-2026-06-23-002 — Web reinicia + memoria alta no upload de NFs Q.P.A.
- **auto_implemented**: true (v2 id 261) · sessao dev 4-maos
- **Fix**: cap `MAX_PDFS_POR_UPLOAD=25` (faturamento_forms) + `gc.collect()`/`f.close()`
  entre arquivos no loop + `with pdfplumber.open(...)` em `danfe_pdf_parser`
  (handle vazava so no happy path). Commit `181aba751`.
- **Pendente (follow-up)**: estrutural — mover lote para worker RQ (1 PDF/job +
  relatorio persistido). A mitigacao reduz o pico (de "soma de N" para ~1-2 em voo).

### [IMPLEMENTADO] IMP-2026-06-23-003 — Upload em lote importa PARCIAL sem erro (data loss)
- **auto_implemented**: true (v2 id 262)
- **Fix**: `except Exception` final + `rollback` + status `falha` visivel + `continue`
  no loop (commit `17d5c4b04`). Data loss por erro logico fechado; residual via OOM
  coberto pela mitigacao do -002.

### [IMPLEMENTADO] IMP-2026-06-23-004 — importar_nf_qpa grava NF com 0 chassis silenciosamente
- **auto_implemented**: true (v2 id 277)
- **Fix**: `_validar_completude_chassis` em `importar_nf_qpa` levanta `NfQpaParseError`
  se 0 chassis OU extraidos < `qtd_declarada_itens_veiculo` (gabarito NCM 8711), ANTES
  de qualquer escrita (espelha o guard de `criar_nf_qpa_de_dados`). Commit `181aba751`,
  10 TDD.
- **Validacao**: PDFs reais 1763/1809/1860 (10/20/16, completos) + **sweep dos 125**
  (122 completos, **0 perda silenciosa**; unica 0-itens = NFd devolucao 140176, esperado).

### [IMPLEMENTADO] IMP-2026-06-23-008 — Tela de NF aceita PDF de CCe e cria NF orfa
- **auto_implemented**: true (v2 id 279)
- **Fix**: porteiro `cce_pdf_extractor.eh_documento_cce` (marcadores RE_FORMATO_QPA/
  MOTOCHEFE) — `importar_nf_qpa` rejeita com `NfQpaDocumentoCceError` (subclasse de
  `NfQpaParseError` -> lote nao perde o arquivo) ANTES do parser/LLM; rota mostra
  status `documento_errado`. Commit `181aba751`.

### [IMPLEMENTADO] IMP-2026-06-23-009 — Parser de CCe classifica correcao de pedido como CHASSI
- **auto_implemented**: true (v2 id 280) · parte de RISCO resolvida
- **Fix**: `cce_service._parece_correcao_de_pedido` + gate em `_tentar_aplicar_cce` —
  quando o texto bruto e "CORRECAO DE PEDIDO" sem "CORRECAO DE CHASSI" (insensivel a
  acento), registra IGNORADA **sem trocar chassis** (o fallback LLM forcava CHASSI por
  mera presenca de chassi). Commit `bd8fe19b2`, 5 TDD.
- **Pendente (follow-up)**: regex SOL puro-numerico do parser de CCe (perde 3/10) —
  unificar com a extracao LLM do parser de NF.

## Parciais / pendentes de decisao (2)

### [RESPONDIDO PARCIAL] IMP-2026-06-23-005 — Backfill de NFs historicas fica NAO_RECONCILIADO
- **auto_implemented**: false (v2 id 278)
- **Causa-raiz isolada** (nao era "falta de modo"): **double-match trap** —
  `ajustar_separacao_pela_nf` (S1=b) cria a sep ja em FATURADA; `_calcular_match` exclui
  FATURADA do JOIN (`nf_qpa_adapter.py:550`) e DESFAZ -> NAO_RECONCILIADO. Confirmado por
  verificacao adversarial + dados PROD (18 NFs split-brain: 260/272 chassis ja FATURADA,
  so o status mentia).
- **PARTE 1 FEITA**: backfill corretivo das 14 NFs split-brain consistentes
  (`scripts/migrations/2026_06_23_backfill_split_brain_nf_qpa.py`, dry-run default/
  idempotente; relink `separacao_item_id` + status BATEU). Aplicado prod:
  `NAO_RECONCILIADO` 120 -> 106. Commit `181aba751`.
- **PARTE 2 PENDENTE (102 NFs sem separacao)** — decisao de modelo (Rafael): faturar
  exige criar separacao (`pedido_id NOT NULL`) e os unicos 7 pedidos vivos sao orders
  atuais (anexar corromperia o tracking). **Caminho escolhido**: subir os pedidos VOE
  historicos (dos PDFs) -> criar separacao VIVA -> match natural (happy path evita o
  trap, sem dado sintetico nem status novo). Cleanup adicional: 22 chassis compartilhados
  entre NFs + 20 em fluxo de venda.

### [RESPONDIDO PARCIAL] IMP-2026-06-23-010 — --faturar-nf-historica afeta fechamento junho
- **auto_implemented**: false (v2 id 281)
- Reconciliado com -005: o "modo novo" pedido e melhor atendido pelo fluxo NORMAL
  retroativo (pedido VOE -> separacao viva -> match), nao por um caminho que pula o
  match. Split-brain ja corrigido (14 NFs).

## Integridade descoberta (fora dos IMP — para revisao Rafael)

- **4 split-brain anomalas** (1727/1729/1737/2037): chassi RE-ALOCADO entre NFs — ex.
  `LA2025SA110004420` faturado p/ NF 1727 (19/06) e re-faturado p/ outra NF (id=25,
  23/06). Uma moto em 2 NFs. NAO corrigido (precisa apontar a dona).
- **NF 1797 / `merged3.pdf`**: 138 paginas / ~9 NFs num unico registro (20 chassis
  unicos). As outras 11 NFs do merge entraram individuais. Split + re-import pendente
  (nao apagar — chassis unicos).

## Resumo
- Avaliados: 7 · Implementados: 5 (002/003/004/008/009) · Parciais: 2 (005/010) · Rejeitados: 0
- Persistencia PROD: 7/7 v2 HTTP 200 (ids 261-262, 277-281)
- Commits (main): `17d5c4b04`, `181aba751`, `bd8fe19b2`
- Backfill prod aplicado: `NAO_RECONCILIADO` 120 -> 106 (14 NFs split-brain -> BATEU)
