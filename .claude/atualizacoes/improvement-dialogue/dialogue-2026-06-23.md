---
date: 2026-06-23
suggestions_evaluated: 6
implemented: 0
rejected: 1
proposed: 5
---

# Improvement Dialogue — 2026-06-23

Backlog de 6 sugestoes IMP avaliaveis (+ 4 F2 de gate humano). Nenhuma auto-implementada:
4 propostas tocam rota/service sensivel (custo de frete, fluxo de dados financeiros, maquina
de estados de portaria), 1 e mitigacao de infra com confound, e 1 e nao-bug. Todas as 6
respostas persistidas no banco (v2 ids 257-262, HTTP 200).

## Sugestoes Avaliadas

### [PROPOSTO] IMP-2026-06-22-001: Skill portaria — dar saida/entrada de embarque pelo chat
- **Categoria**: skill_suggestion · **Severidade**: warning
- **Usuario de origem**: Samantha Zampieri do AMARAL (id 49)
- **Decisao**: respondido · **Implementado**: nao (proposta)
- **Arquivos afetados**: nenhum (proposta)
- **Notas**: Gap VALIDO — operador sem acesso ao modulo Portaria nao consegue dar saida pelo
  chat; skill `operando-portaria` NAO existe. NAO auto-implementavel: a cadeia de saida
  (carimba `Embarque.data_embarque` + propaga `Separacao` por-CD + sincroniza `EntregaMonitorada`
  + dispara 3 hooks de frete Nacom/CarVia/Op.Assai) vive EMBUTIDA e DUPLICADA em
  `app/portaria/routes.py:registrar_movimento` e `adicionar_embarque` (core proibido). Plano:
  PASSO 1 extrair `app/portaria/services/movimento_service.py` (toca routes.py = aval); PASSO 2
  skill WRITE que invoca o service (dry-run default, `--confirmar`, `--user-id`, resolve motorista
  via `Motorista.buscar_por_cpf`, valida `local_cd` VM/TM, alerta saida POR-CD em embarque misto).
  `ControlePortaria.motorista_id` e NOT NULL — o caso da Samantha era justamente nao ter os dados
  do motorista, entao a skill precisa resolver/cadastrar motorista. Ocorrencia unica.

### [PROPOSTO] IMP-2026-06-22-002: Botao excluir fatura transportadora CarVia (PENDENTE sem vinculos)
- **Categoria**: skill_suggestion · **Severidade**: warning
- **Usuario de origem**: TALITA DE LE LIMA (id 17)
- **Decisao**: respondido · **Implementado**: nao (proposta — rota nova)
- **Arquivos afetados**: nenhum (proposta)
- **Notas**: Gap VALIDO — nao ha rota para excluir `CarviaFaturaTransportadora` segura. Requer
  rota nova (core) -> proposta. Padrao xerox: `excluir_despesa_extra_carvia`
  (`custo_entrega_routes.py:1877`). Plano: `POST /faturas-transportadora/<id>/excluir` em
  `fatura_routes.py` com 6 guards (sistema_carvia; status_conferencia=PENDENTE;
  status_pagamento=PENDENTE; NOT conciliado/sem `CarviaConciliacao`; 0 itens; 0 subcontratos),
  delete+commit em try/except, botao condicional via `@property pode_excluir()`. Caso: fatura
  ID 90 (DAGO TRANSPORTE) duplicada, PENDENTE, 0 vinculos.

### [PROPOSTO] IMP-2026-06-22-004: Peso cubado inconsistente em frete CarVia multi-NF (BUG REAL)
- **Categoria**: gotcha_report · **Severidade**: warning
- **Usuario de origem**: Barbara Ferreira Amaral (id 87)
- **Decisao**: respondido · **Implementado**: nao (proposta — service de custo + rota backfill)
- **Arquivos afetados**: nenhum (proposta)
- **Notas**: **BUG REAL confirmado por leitura de codigo + dados Render.** Causa-raiz isolada:
  HA DOIS caminhos de calculo de peso cubado que divergem. (A) FONTE CANONICA (tela/exports,
  R1/R19) `MotoRecognitionService.calcular_peso_cubado_batch` via `CarviaNfItem.modelo_moto_id`
  (independe da cotacao). (B) CALCULO DO FRETE `_peso_cubado_resolvido`
  (`carvia_frete_service.py:513-601`) -> `calcular_cubado_por_modelos`
  (`embarque_carvia_service.py:604-641`) via `CarviaCotacaoMoto` da COTACAO: se a cotacao NAO tem
  o modelo cadastrado com cubado>0, `resolver_modelo_em_lista` retorna None -> soma 0 ->
  `max(bruto,0)=bruto`. Prova: NF 38312 (JET, id 326) e 38317 (DOT, id 330) AMBAS tem item com
  modelo + veiculos com modelo no banco — logo NAO e "NF sem veiculo"; a divergencia so pode vir
  da cotacao nao cobrir o JET. Por isso a tela mostra cubagem nas duas e o frete gravou bruto so
  na JET (675,92 vs 867,54 correto). Bug secundario: `frete_routes.py:627` (backfill POST) usa
  `sum(peso_bruto)` ignorando cubagem. Correcao: unificar o ramo "item real" de
  `_peso_cubado_resolvido` para usar a fonte canonica `calcular_peso_cubado_nf` + corrigir o POST
  backfill com `max(bruto,cubado)` + backfill dos fretes ja gravados + teste de regressao.
  **RECONCILIACAO COM HISTORICO**: 3a re-incidencia do mesmo problema-raiz — #32 (2026-05-29,
  rejeitada: formula proposta incorreta) e #33 (2026-06-02, proposta: "cotacao nao cobre modelo
  MIA MOTO CHEFE"). O #33 ja apontava a causa (cotacao incompleta); o VALOR NOVO aqui e a rota de
  correcao concreta: o sistema JA TEM um caminho canonico robusto (`calcular_peso_cubado_batch`
  via item) usado na tela/export — basta o frete passar a usa-lo em vez do caminho fragil
  dependente da cotacao. Encerra a re-incidencia em vez de so re-propor.

### [REJEITADO] IMP-2026-06-23-001: list_session_uploads nao recupera uploads da sessao rotacionada
- **Categoria**: skill_bug · **Severidade**: warning
- **Usuario de origem**: Rayssa Alves (id 78)
- **Decisao**: rejeitado · **Implementado**: nao
- **Arquivos afetados**: nenhum
- **Notas**: **NAO E BUG no codigo atual.** Limitacao historica de timing de deploy. O indice
  consultado por `list_session_uploads` (`session_search_tool.py:1180`) ->
  `listar_uploads_usuario` (`upload_recovery_service.py:70-82`) e a tabela PERSISTENTE
  `agente_upload` (filtro user_id+ativo+janela CORRETO). Essa tabela so e populada pelo dual-write
  `persistir_upload_s3`, introduzido em 20/06 ~16:50-16:59 (commits f4c8002be/7744aa372/d81a3e89d).
  Os anexos da Rayssa (18-19/06 e 20/06 de manha) foram salvos pelo codigo ANTIGO (so `file.save`
  em /tmp, sem INSERT) — a tabela nem existia em 18/06. A query retorna 0 porque NAO HA linhas,
  nao por bug. Worktree `s3-uploads-agente-recuperacao` ja mergeada em main; para uploads
  pos-deploy a recuperacao funciona. Solucao do caso Rayssa = pedir reenvio (o proprio fallback do
  `resume_fallback_notice`). Nota lateral (read-only, nao bug): `persistir_upload_s3` retorna None
  silenciosamente se `USE_S3=false` — vale um spot-check do `USE_S3` em prod (esta ON, a feature
  funcionou pos-deploy). Liga ao IMP-2026-06-20 (aviso de anexos perdidos, #43) e ao #44 (causa
  "filesystem isolado" refutada).

### [PROPOSTO] IMP-2026-06-23-002: Web reinicia + memoria alta no upload de NFs Q.P.A.
- **Categoria**: gotcha_report · **Severidade**: warning
- **Usuario de origem**: Rayssa Alves (id 78)
- **Decisao**: respondido · **Implementado**: nao (proposta — mitigacao)
- **Arquivos afetados**: nenhum (proposta)
- **Notas**: CONFIRMADO por codigo que o upload e SINCRONO no request: `importar_nf_qpa` ->
  `DanfePDFParser` carrega o PDF inteiro em memoria (`pdfplumber.open(io.BytesIO(...))`) + ate 3
  chamadas LLM sincronas/arquivo (Haiku->Sonnet->Sonnet) + upload S3 sincrono; nenhum worker
  async no caminho (`faturamento.py:318-389`). Risco real de pico de memoria (722->882MB
  observado). CONFOUND (2.1.1): a evidencia diz "sem deploy entre os restarts 09:30/36/48, sem
  SIGKILL/OOM/Traceback" — CONSISTENTE com recycle por memoria mas NAO prova; **acao Rafael:
  confirmar no Render se foi crash ou restart manual**. TRABALHO DEV EM CURSO (2.1.1a): WIP nao
  commitado em `nf_qpa_adapter.py` (`_to_decimal_safe`) corrige UMA causa de excecao mas nao o
  sincronismo. Mitigacao proposta: (1) imediata — limitar nº de PDFs/request + orientar lotes
  menores; (2) estrutural — mover o lote para worker RQ com relatorio por-arquivo persistido (casa
  com IMP-2026-06-23-003); (3) liberar BytesIO/texto entre arquivos. Ligada a IMP-2026-06-23-003.

### [PROPOSTO] IMP-2026-06-23-003: Upload em lote NF Q.P.A. importa PARCIAL sem erro (data loss)
- **Categoria**: gotcha_report · **Severidade**: warning
- **Usuario de origem**: Rayssa Alves (id 78)
- **Decisao**: respondido · **Implementado**: nao (proposta — toca a rota)
- **Arquivos afetados**: nenhum (proposta)
- **Notas**: **BUG REAL (data loss silencioso) confirmado.** `faturamento.py:331-363`: o loop
  `for f in arquivos` so captura `NfQpaJaImportadaError` e `NfQpaParseError`. QUALQUER outra
  excecao (`sqlalchemy.exc.*`, `decimal.InvalidOperation`, `anthropic.APIError`, `AttributeError`
  em services internos) PROPAGA para fora do loop -> arquivos restantes nao processados -> 500 SEM
  relatorio. Como `importar_nf_qpa` commita POR ARQUIVO (`nf_qpa_adapter.py:490`), os ja
  processados ficam e os demais somem sem rastro (10 PDFs -> 4 importados, 4 sumiram). O "sem erro
  visivel" e consistente com worker reciclado por memoria no meio do request (ver IMP-...-002).
  WIP em curso (2.1.1a): `_to_decimal_safe` ja blinda UMA causa (`InvalidOperation` em valor_total,
  ex.: PDF de CCe no endpoint de NF) mas e pontual. Fix estrutural proposto: (1) `except Exception`
  final no loop registrando o arquivo como `status='falha'` e CONTINUANDO — transforma data loss
  em linha visivel no relatorio; (2) idealmente mover o lote para worker RQ com relatorio
  por-arquivo PERSISTIDO (ok/duplicada/erro_parse/falha-infra); (3) mitigacao imediata: 1-3 PDFs
  por vez.

## Sugestoes de skill F2 aguardando decisao Rafael+CC

> Gate humano (`adhoc-`/`skill-gap-`) — `python scripts/agente/revisar_sugestoes_skill.py listar`.
> NAO decididas nem persistidas. 3 sao reincidentes de #44/#45; 1 e nova.

- `adhoc-cluster-1070` (skill_suggestion, info — **TALITA id 17**, 23 membros, 5 dias):
  gerar Embarque programaticamente + `processar_lancamento_automatico_fretes` em sequencia (gap
  em `gerindo-expedicao`). Reincidente (#44/#45); liga a IMP-2026-06-16-003 (#40, toca
  `app/embarques/routes.py`) e ao falso-positivo do embarque ad-hoc (#43, worktree
  `feat/redespacho-multi-perna`).
- `skill-gap-lendo-arquivos-…` (skill_suggestion, info — **Rayssa id 78**, 2 membros, 4 dias):
  analise exploratoria sobre DataFrame (distribuicao categorica, completude, relatorio de
  qualidade) estendendo `lendo-arquivos`. Reincidente (#44/#45). Baixa frequencia (2).
- `adhoc-cluster-1161` (skill_suggestion, info — **TAMIRIS id 44**, 64 membros, 3 dias):
  reclassificacao em lote de AMLs origem->destino no Odoo. Contraparte executora da decisao #163
  (ja analisada no #44, matriz 2.1.1: write 100% ad-hoc, virar skill executora rejeitado;
  salvaguarda `monitorar-andamento`/`validar-lote` ja em `auditando-reclassificacao-odoo`).
  Reabrir a decisao e do Rafael.
- `skill-gap-exportando-arquivos-…` (skill_suggestion, info — **GISLENE id 47**, 2 membros, 1 dia)
  **[NOVA]**: exportar N pedidos especificos via SQL avancado (CTEs/JOINs) para Excel
  (carteira+faturamento), estendendo `exportando-arquivos`. Baixa frequencia (2).

## Resumo
- Total avaliadas: 6
- Implementadas automaticamente: 0
- Rejeitadas: 1 (IMP-2026-06-23-001 — limitacao historica de deploy, nao bug)
- Propostas para revisao humana: 5 (1 skill via refactor de service, 1 rota CarVia, 1 bug de custo
  de frete re-incidente #32/#33, 1 mitigacao de memoria com confound, 1 fix de data loss)
- F2 listadas (gate humano): 4 (3 reincidentes + 1 nova)
- Persistencia no banco: 6/6 v2 HTTP 200 (ids 257-262)
- Branch: `cron/manutencao` (worktree, sem push)
