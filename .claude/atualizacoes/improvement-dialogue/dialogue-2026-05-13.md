---
date: 2026-05-13
suggestions_evaluated: 10
implemented: 10
rejected: 0
proposed: 0
---

# Improvement Dialogue — 2026-05-13

Sessao alvo D8: 10 sugestoes pendentes do Agent SDK, todas implementadas. Cluster
principal: 4 sugestoes sobre o evaluator da `consultando-sql` (UPDATE bloqueado
+ falso positivo de data DD/MM/YYYY em VARCHAR), 2 sobre o pipeline de artifacts
(Parcel + Vite 7 conflict) e 4 sobre comportamento do agente (baseline cache,
tabela D-0, diagnostico cauteloso, qtd_saldo=0). Sem rejeitar nenhuma; sem propor
mudanca em core files.

## Sugestoes Avaliadas

### [RESPONDED] IMP-2026-05-13-004: consultando-sql evaluator bloqueia UPDATE mesmo com INSERT aprovado
- **Categoria**: skill_bug
- **Severidade**: critical
- **Decisao**: respondido
- **Implementado**: sim
- **Arquivos afetados**: `.claude/skills/consultando-sql/scripts/text_to_sql.py`
- **Notas**: `SQLEvaluator.evaluate()` agora aceita `admin_mode=bool`. Em admin
  mode, a rule 9 do prompt aplica criterio UNIFORME: SELECT/INSERT/UPDATE/DELETE
  autorizados; DROP/ALTER/TRUNCATE/funcoes perigosas continuam bloqueadas. Linha
  1225 atualizada para passar `admin_mode` na chamada. Causa raiz: Haiku
  nondeterministic — sem flag explicita, evaluator podia aprovar INSERT e
  reprovar UPDATE no mesmo run.

### [RESPONDED] IMP-2026-05-13-007: Evaluator bloqueia UPDATE apos aprovar INSERT (DML inconsistente)
- **Categoria**: gotcha_report
- **Severidade**: critical
- **Decisao**: respondido (duplicata tecnica de IMP-004)
- **Implementado**: sim (no escopo de IMP-004)
- **Arquivos afetados**: `.claude/skills/consultando-sql/scripts/text_to_sql.py`
- **Notas**: Mesma session_id (eb1ad77d) e mesma causa de IMP-004. Fix unificado
  ja cobre ambas — prompt do evaluator passou a tratar todos os verbos DML com
  o mesmo criterio em admin_mode.

### [RESPONDED] IMP-2026-05-13-005: bundle-artifact.sh @parcel/config-default ausente
- **Categoria**: gotcha_report
- **Severidade**: critical
- **Decisao**: respondido (causa raiz coberta por IMP-006)
- **Implementado**: sim (no escopo de IMP-006)
- **Arquivos afetados**: `.claude/skills/gerando-artifact/scripts/init-artifact.sh`
- **Notas**: Causa raiz identificada como Vite 7 + Rolldown/OXC quebrando
  peer-deps do Parcel 2.12 — `npm --legacy-peer-deps` suprime erro e nao linka
  tarballs em `node_modules/@parcel/`. Bundle-artifact.sh ja tem 3 fallbacks de
  retry, mas o fix real e o pin do Vite (IMP-006). Adicionar `@parcel/config-default`
  como devDependency no template seria redundante (parcel resolve via require
  interno). Tech debt registrado: migrar para `vite-plugin-singlefile`.

### [RESPONDED] IMP-2026-05-13-006: init-artifact.sh pinar Vite 5.4.11 sempre
- **Categoria**: gotcha_report
- **Severidade**: critical
- **Decisao**: respondido
- **Implementado**: sim
- **Arquivos afetados**: `.claude/skills/gerando-artifact/scripts/init-artifact.sh`
- **Notas**: Removido branch condicional por NODE_VERSION. `VITE_VERSION="5.4.11"`
  fixo, com bloco de comentario documentando o conflito Vite 7/Rolldown vs
  Parcel 2.12. Etapa de pin (`npm install -D vite@$VITE_VERSION`) agora roda em
  todas as versoes de Node (antes era apenas Node<20). Sintaxe shell validada via
  `bash -n`. Migracao para `vite-plugin-singlefile` registrada como tech debt no
  comentario.

### [RESPONDED] IMP-2026-05-13-001: baseline nao revalida em solicitacao duplicada
- **Categoria**: gotcha_report
- **Severidade**: warning
- **Decisao**: respondido
- **Implementado**: sim
- **Arquivos afetados**: `.claude/skills/gerando-baseline-conciliacao/SKILL.md`
- **Notas**: Adicionada secao "Revalidacao em Solicitacoes Repetidas". Limite:
  `<60s` reaproveita (defesa contra duplo-clique); `>=60s` SEMPRE revalida e
  reporta delta. Output obrigatorio inclui linha `Revalidado as HH:MM — delta
  vs execucao anterior: <DELTA> linhas`. Anti-padrao "Essa solicitacao foi
  processada ha 2 min" documentado como proibido.

### [RESPONDED] IMP-2026-05-13-002: baseline nao inclui tabela D-0 automaticamente
- **Categoria**: instruction_request
- **Severidade**: warning
- **Decisao**: respondido
- **Implementado**: sim (textual; gerar_baseline.py nao alterado nesta rodada)
- **Arquivos afetados**: `.claude/skills/gerando-baseline-conciliacao/SKILL.md`
- **Notas**: Quando `data_referencia == hoje`, agente DEVE incluir tabela D-0
  (conciliacoes ja realizadas no dia, por usuario) alem da D-1. Se vazia: exibir
  "Nenhuma conciliacao registrada hoje ate HH:MM". Excel mantem aba canonica
  "Conciliacoes Dia Anterior" com subsecao "Hoje (D-0)" abaixo de "Ontem (D-1)".
  **Follow-up**: estender `gerar_baseline.py` para emitir D-0 e D-1 no JSON de
  saida (atualmente o agente precisa compor via SQL adicional).

### [RESPONDED] IMP-2026-05-13-003: evaluator falso positivo data DD/MM/YYYY em VARCHAR
- **Categoria**: skill_bug
- **Severidade**: warning
- **Decisao**: respondido
- **Implementado**: sim
- **Arquivos afetados**: `.claude/skills/consultando-sql/scripts/text_to_sql.py`
- **Notas**: Rule 4 do prompt do `SQLEvaluator` reescrita: SEMPRE consultar tipo
  REAL da coluna no schema ANTES de validar formato; campo varchar/text que
  armazena data (`data_agenda VARCHAR(10)` com DD/MM/YYYY) deve ACEITAR string
  consistente com o padrao dos registros. Restricao do formato ISO so para
  tipos DATE/TIMESTAMP reais. Bloco com exemplo explicito do falso-positivo a
  evitar foi adicionado.

### [RESPONDED] IMP-2026-05-13-008: evaluator presume tipo DATE para campos VARCHAR
- **Categoria**: gotcha_report
- **Severidade**: warning
- **Decisao**: respondido (duplicata de IMP-003)
- **Implementado**: sim (no escopo de IMP-003)
- **Arquivos afetados**: `.claude/skills/consultando-sql/scripts/text_to_sql.py`
- **Notas**: Mesma causa de IMP-003. Fix unico cobre ambas.

### [RESPONDED] IMP-2026-05-13-009: diagnostico tecnico contraditorio
- **Categoria**: gotcha_report
- **Severidade**: warning
- **Decisao**: respondido
- **Implementado**: sim
- **Arquivos afetados**: `app/agente/prompts/system_prompt.md`
- **Notas**: Ampliada L2 (ETICA) na `constitutional_hierarchy`: NUNCA afirmar
  causa raiz com confianca a partir de logs INDIRETOS sem isolar; etiquetar
  explicitamente "Hipotese:" vs "Confirmado:"; se diagnostico mudar apos nova
  evidencia, ANUNCIAR a revisao ANTES que usuario cobre (formato: "Revisao: a
  hipotese anterior X estava incorreta porque Y. Nova hipotese: Z").

### [RESPONDED] IMP-2026-05-13-010: qtd_saldo=0 em embarque sem confirmacao tipada
- **Categoria**: instruction_request
- **Severidade**: warning
- **Decisao**: respondido
- **Implementado**: sim
- **Arquivos afetados**: `app/agente/prompts/system_prompt.md`
- **Notas**: Adicionada regra R3.1 dentro de R3 (Confirmacao Obrigatoria): ao
  inserir separacao com `qtd_saldo=0` em embarque ainda nao faturado
  (`sincronizado_nf=False`), levantar ATENCAO explicita e exigir confirmacao
  TIPADA A/B/C com justificativa registrada em `embarque_item.observacao`.
  Cenarios: (A) saida fisica ja ocorreu — reabrir item para correcao de NF,
  (B) zerada intencionalmente, (C) erro — abortar.

## Resumo
- Total avaliadas: 10
- Implementadas automaticamente: 10
- Rejeitadas: 0
- Propostas para revisao humana: 0
- Arquivos modificados: 4 (`text_to_sql.py`, `init-artifact.sh`, `gerando-baseline-conciliacao/SKILL.md`, `system_prompt.md`)
- Sintaxe validada: `py_compile` (text_to_sql.py) + `bash -n` (init-artifact.sh)
- Follow-up: estender `gerar_baseline.py` para emitir tabelas D-0 e D-1 separadas no JSON
