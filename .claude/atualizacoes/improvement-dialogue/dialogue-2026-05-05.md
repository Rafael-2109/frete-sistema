---
date: 2026-05-05
suggestions_evaluated: 1
implemented: 0
rejected: 0
proposed: 1
---

# Improvement Dialogue — 2026-05-05

## Sugestoes Avaliadas

### [PROPOSED] IMP-2026-05-01-001: Vincular NFe TagPlus ao pedido de venda original (read:pedidos / read:vendas)

- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Decisao**: respondido com proposta (auto_implemented=false)
- **Implementado**: nao — plano detalhado para revisao humana
- **Arquivos afetados** (planejados, nao modificados nesta execucao):
  - `app/hora/models/tagplus.py` — adicionar campos novos em `HoraTagPlusNfeEmissao` e/ou `HoraTagPlusToken` (RESTRITO)
  - `app/hora/services/tagplus/backfill_service.py` — extrair e persistir `pedido_os_vinculada.id`
  - `app/hora/services/tagplus/oauth_client.py` — armazenar `scope` retornado no token response
  - `app/hora/routes/tagplus_routes.py` — adicionar check de scope mismatch no checklist (RESTRITO)
  - `app/templates/hora/tagplus/checklist.html` — exibir alerta vermelho de scope desatualizado
  - `scripts/migrations/hora_21_tagplus_pedido_id.{py,sql}` — migration nova (DDL)

#### Validacao contra codebase

Confirmacoes (todas as afirmacoes da sugestao foram verificadas):

1. **Backfill descarta pedido_os_vinculada — CONFIRMADO**.
   `app/hora/services/tagplus/backfill_service.py:656` (`importar_nfe_da_api`) le GET `/nfes/{id}`
   mas o log/persistencia nao referencia `pedido_os_vinculada` nem `numero_pedido` da NFe TagPlus.
   `grep -n "pedido_os_vinculada" app/hora/services/tagplus/backfill_service.py` retorna 0 matches.

2. **Modelo nao tem campo `tagplus_pedido_id` — CONFIRMADO**.
   `app/hora/models/tagplus.py:186-247` (`HoraTagPlusNfeEmissao`) nao define coluna `tagplus_pedido_id`.
   Campos existentes: `tagplus_nfe_id`, `numero_nfe`, `serie_nfe`, `chave_44`, `protocolo_aprovacao`.

3. **Checklist nao compara scope contratado vs scope vigente — CONFIRMADO**.
   `app/hora/routes/tagplus_routes.py:195-280` (`tagplus_checklist`) so usa `conta.scope_contratado`
   na string informativa (linha 208); nao compara com scope efetivo do token. `HoraTagPlusToken`
   (`app/hora/models/tagplus.py:74-101`) nao guarda o scope retornado pelo OAuth.

4. **scope_contratado e usado no OAuth flow — CONFIRMADO**.
   `app/hora/services/tagplus/oauth_client.py:76` envia `scope=self.conta.scope_contratado` no
   request, mas a resposta do TagPlus pode retornar scope diferente (exatamente o que a sugestao
   reporta para o caso real: scope_contratado atualizado em 01/05/2026 mas token vigente foi
   gerado com scope antigo).

#### Por que NAO foi auto-implementado

- A sugestao requer mudanca em `models/tagplus.py` (nova coluna + nova relacao opcional) — escopo RESTRITO em D8.
- Adiciona rota/logica em `routes/tagplus_routes.py` — escopo RESTRITO em D8.
- Demanda nova migration (DDL `ALTER TABLE`), que precisa ser aprovada e rodada em producao com
  cuidado — preferencia humana.
- O Nivel 2 (chamadas a `/pedidos/{id}` e `/vendas/{id}`) requer **reautorizacao OAuth** com
  Authorization Code Flow novo — acao operacional fora do escopo D8 (admin TagPlus precisa autorizar
  na UI do portal). O `refresh_token` nao re-emite scope (confirmado empiricamente na sessao
  `1a854db0-270e-4f75-9b9c-4671e8990939` em 01/05/2026).

#### Plano detalhado (4 fases)

**Fase 1 — Persistir `tagplus_pedido_id` no backfill (Nivel 1, sem novo OAuth)**

1. Adicionar coluna em `HoraTagPlusNfeEmissao`:
   ```python
   tagplus_pedido_id = db.Column(db.Integer, nullable=True, index=True)
   # ID interno do pedido TagPlus extraido de pedido_os_vinculada.id na NFe.
   ```
2. Migration `scripts/migrations/hora_21_tagplus_pedido_id.{py,sql}`:
   - SQL: `ALTER TABLE hora_tagplus_nfe_emissao ADD COLUMN IF NOT EXISTS tagplus_pedido_id INTEGER;`
     `CREATE INDEX IF NOT EXISTS idx_hora_tagplus_nfe_emissao_tagplus_pedido_id ON hora_tagplus_nfe_emissao(tagplus_pedido_id);`
   - Python: usar `create_app()` + `db.session.execute()` com verificacao before/after (regra
     `~/.claude/CLAUDE.md` secao MIGRATIONS).
3. Patch em `backfill_service.importar_nfe_da_api` (apos resolver/atualizar a venda; antes do commit final):
   ```python
   pedido_os = (nfe.get('pedido_os_vinculada') or {}) if isinstance(nfe.get('pedido_os_vinculada'), dict) else {}
   tagplus_pedido_id = pedido_os.get('id') if pedido_os else None
   if venda and tagplus_pedido_id:
       emissao = HoraTagPlusNfeEmissao.query.filter_by(venda_id=venda.id).first()
       if emissao and emissao.tagplus_pedido_id != tagplus_pedido_id:
           emissao.tagplus_pedido_id = tagplus_pedido_id
           # commit feito junto com o restante do upsert
   ```
4. Backfill retroativo (script one-shot opcional): rodar contra todos os
   `HoraTagPlusNfeEmissao.status = 'APROVADA'` que tem `tagplus_nfe_id` e re-chamar
   `GET /nfes/{id}` para popular o campo retroativamente.

**Fase 2 — Detectar scope mismatch no checklist**

1. Adicionar coluna `scope_efetivo` em `HoraTagPlusToken`:
   ```python
   scope_efetivo = db.Column(db.String(255), nullable=True)
   # Scope retornado pelo TagPlus no callback OAuth. Pode divergir de
   # HoraTagPlusConta.scope_contratado se a admin atualizou o contratado mas
   # ainda nao reautorizou.
   ```
2. Em `oauth_client.py`, ao receber `token_response`, persistir
   `token.scope_efetivo = token_response.get('scope')`.
3. Em `tagplus_checklist`, adicionar check novo:
   ```python
   if conta and conta.token:
       scope_token = (conta.token.scope_efetivo or '').strip()
       scope_contratado = (conta.scope_contratado or '').strip()
       if scope_token and scope_token != scope_contratado:
           checks.append({
               'item': 'Scope OAuth atualizado',
               'status': 'erro',
               'mensagem': (
                   f'scope contratado={scope_contratado!r} difere do '
                   f'scope vigente do token={scope_token!r}. '
                   'Refresh nao basta — refazer Authorization Code Flow '
                   '(Conta -> Reautorizar OAuth) para ativar novos scopes '
                   '(read:pedidos, read:vendas).'
               ),
           })
       elif scope_token:
           checks.append({
               'item': 'Scope OAuth atualizado',
               'status': 'ok',
               'mensagem': f'scope vigente={scope_token!r} (em sincronia com contratado).',
           })
   ```
4. Template `checklist.html` ja renderiza qualquer check adicionado — sem mudanca necessaria
   alem da rota.

**Fase 3 — Habilitar GET /pedidos/{id} (Nivel 2)** — apos admin reautorizar OAuth com escopo `read:pedidos read:vendas`

1. Criar `app/hora/services/tagplus/pedido_service.py`:
   ```python
   def importar_pedido_tagplus(api: ApiClient, pedido_id: int) -> dict:
       """GET /pedidos/{id} -> dict com cliente, itens, vendedor, condicao_pagamento, datas, valor.
       Caller decide o que enriquecer em HoraVenda."""
       r = api.get(f'/pedidos/{pedido_id}')
       if r.status_code == 401:
           raise ScopeInsuficienteError('GET /pedidos requer read:pedidos.')
       if r.status_code != 200:
           raise PedidoNaoEncontrado(f'GET /pedidos/{pedido_id} -> HTTP {r.status_code}')
       return r.json()
   ```
2. Em `backfill_service.importar_nfe_da_api`, quando `tagplus_pedido_id` presente E scope efetivo
   inclui `read:pedidos`: chamar `importar_pedido_tagplus` e enriquecer:
   - `HoraVenda.vendedor` (se NULL)
   - `HoraVenda.forma_pagamento` (se default `NAO_INFORMADO`)
   - `HoraVendaDivergencia` para divergencias entre NFe e pedido (preco, item, data).
3. Feature flag para gating: `HORA_TAGPLUS_ENRICH_PEDIDO=false` por default; admin liga apos
   reautorizar.

**Fase 4 — UI: tela de detalhe da venda mostra link para pedido TagPlus**

- Em `templates/hora/venda_detalhe.html`, quando `emissao.tagplus_pedido_id`, renderizar
  badge "Pedido TagPlus #{tagplus_pedido_id}" com link para tela nova
  `/hora/tagplus/pedidos/{tagplus_pedido_id}` (lazy fetch via `pedido_service`).

#### Riscos e mitigacoes

| Risco | Mitigacao |
|---|---|
| Migration adicionar coluna sem default rapido em tabela grande | Coluna `nullable=True` sem default — apenas registra nova metadata em rows futuras. Backfill retroativo opcional. |
| `pedido_os_vinculada` ausente em algumas NFes (vendas sem pedido) | Checagem `if pedido_os_vinculada:` antes de usar; campo permanece NULL para NFes sem vinculo. |
| Reautorizacao OAuth pode quebrar token vigente | Documentar no checklist: "Reautorizar gera novo token; antigo invalidado pelo TagPlus." Admin deve reautorizar fora de horario de pico. |
| `scope_efetivo` pode estar vago em tokens existentes | Migration cria coluna NULL; comparacao trata `''` como "desconhecido" e exibe aviso ("scope vigente nao registrado — refazer OAuth para sincronizar"). |

#### Notas

- Sugestao 100% valida: codigo confirmou todas as afirmacoes (3 grep checks + 4 reads).
- Plano respeita invariantes do modulo HORA (`app/hora/CLAUDE.md`): nao toca em `hora_moto`,
  nao adiciona FK cross-modulo, nao bypassa decorator `require_hora_perm`.
- Migration segue padrao dual (Python + SQL idempotente) conforme regra global.
- Fase 1 e independente de Fase 2/3/4 — pode ser implementada e deployada isoladamente.

## Resumo

- Total avaliadas: 1
- Implementadas automaticamente: 0
- Rejeitadas: 0
- Propostas para revisao humana: 1

**Justificativa para 0 auto-implementadas**: a unica sugestao da fila tinha 3 areas RESTRITAS
(`models/tagplus.py`, `routes/tagplus_routes.py`, migration nova). Plano detalhado entregue
para humano executar em sessao dedicada (provavelmente integrado ao roadmap TagPlus do modulo HORA).
