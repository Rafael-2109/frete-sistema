---
date: 2026-06-29
suggestions_evaluated: 2
implemented: 1
rejected: 0
proposed: 1
---

# Improvement Dialogue — 2026-06-29

> Cron D8 #53. Backlog do PASSO 1 = 6 sugestoes: 2 avaliaveis (prefixo `IMP-`) + 4 F2
> (prefixos `adhoc-`/`skill-gap-`, gate humano — apenas listadas). PASSO 1.5
> (reconciliacao de propostas v2 sem v3, ultimos 45d) = 0 linhas, nada a reconciliar.

## Sugestoes Avaliadas

### [RESPONDED ✅ auto-implementado] IMP-2026-06-29-001: DanfePDFParser — 3 bugs layout Consisanet (Alisul SC), NF 986
- **Categoria**: skill_bug
- **Severidade**: critical
- **Usuario de origem**: Rafael De Carvalho Nascimento (id 1)
- **Decisao**: respondido
- **Implementado**: sim
- **Arquivos afetados**:
  - `app/carvia/services/parsers/danfe_pdf_parser.py`
  - `tests/carvia/test_danfe_consisanet_layout.py` (novo)
- **Notas**:
  Os 3 bugs descritos foram **confirmados no codigo do repositorio** — o agente SDK roda em
  producao e NAO edita codigo-fonte (corrigiu apenas o registro da NF 986 via workaround inline),
  entao o parser versionado seguia bugado. Bug REAL pela matriz 2.1.1 (codigo do fluxo oficial
  erra). Fixes aplicados exatamente como especificado na sugestao (ja validados na sessao real
  contra a NF 986):
  1. **`get_numero_nf` Strategy 1**: regex `N[°ºo.]` → `N(?:[°ºo.]|UM\.?)`. O `u` de "Num." nao
     estava em `[°ºo.]`; agora cobre `Nº/N°/N.` + `NUM.`/`NUM` (IGNORECASE resolve case).
  2. **`get_valor_total` Strategy 2**: reescrita para iterar TODAS as ocorrencias de
     "VALOR TOTAL DA NOTA" e pular a do canhoto (proxima linha contem "RIE" de "Serie" → so token
     era a serie "1" → retornava 1.0). Novo helper `_encontrar_linhas()` (plural).
  3. **`_parsear_linha_produto` WITH-CFOP**: regex `\s+(\w{1,5})` → `\s*([A-Za-z]+)`. Aceita
     CFOP+UNIDADE colados ("6403UNIDADE") e unidades longas; retro-compativel com "5405 UN ...".
  **Verificacao**: novo `tests/carvia/test_danfe_consisanet_layout.py` (4 casos: numero_nf='986',
  valor_total=16473.63, 1 item com ncm=87116000/cfop=6403/unidade=UNIDADE, + retrocompat CFOP/UN
  com espaco). Suite parser: **23 passed** (4 Consisanet + 19 Bling, zero regressao).
  Persistido v2 id 338 (HTTP 200).

### [RESPONDED 📋 proposta] IMP-2026-06-29-002: Skill/rota de reprocessar NF CarVia existente
- **Categoria**: skill_suggestion
- **Severidade**: warning
- **Usuario de origem**: Rafael De Carvalho Nascimento (id 1)
- **Decisao**: respondido (proposta)
- **Implementado**: nao
- **Arquivos afetados**: nenhum (proposta)
- **Notas**:
  Skill `reprocessando-nf-carvia` NAO existe (confirmado). Sugestao **valida** e diretamente
  ligada ao IMP-001 (parser bugado deixa NFs com `valor_total` errado, `numero_nf=None` e 0 itens
  sem via de recuperacao). Decisao = **propor**, nao auto-implementar, porque (a) e feature
  multi-arquivo que toca o NUCLEO do pipeline de importacao (gravacao de itens/veiculos +
  MotoRecognition) e (b) a verificacao real (precision-engineer §5) exige baixar o PDF da NF 598
  do S3 e rodar end-to-end — fora do alcance seguro do cron autonomo. A propria sugestao lista
  "rota POST" como candidato (rotas = APENAS PROPOR pelos guardrails).

  **Plano acionavel** (todos os pontos de reuso ja existem — file:line verificados):
  1. Extrair `reprocessar_nf(nf_id, criado_por) -> dict(diff)` em
     `app/carvia/services/parsers/importacao_service.py` (services/*.py = auto-implementavel):
     - `nf = CarviaNf.query.get(nf_id)`; snapshot ANTES (numero_nf, valor_total, peso_bruto, nº itens).
     - baixar PDF: `get_file_storage().read(nf.arquivo_pdf_path)` (campo em `models/documentos.py:50`).
     - `data = self._parsear_danfe_pdf(conteudo, nf.arquivo_nome_original)` (`importacao_service.py:1621`).
     - `self._atualizar_nf_existente(nf, data, criado_por)` (ja existe, `:1938` — COALESCE dos campos).
     - itens: `CarviaNfItem.query.filter_by(nf_id=nf.id).delete()` (idempotente) + reinserir de
       `data['itens']` espelhando `salvar_importacao:575-585` (8 chaves: codigo_produto, descricao,
       ncm, cfop, unidade, quantidade, valor_unitario, valor_total_item).
     - veiculos: de `data['veiculos']`, dedup por chassi UNIQUE (espelhar `:596-609`).
     - `MotoRecognitionService().detectar_e_persistir_nf(nf.id)` (ja existe, `moto_recognition_service.py:512`).
     - commit; retornar diff antes/depois.
  2. Expor por UMA via (decisao Rafael na 4-maos):
     - **Skill** `reprocessando-nf-carvia` (SKILL.md + script CLI fino sob app_context) — auto-implementavel; OU
     - **Rota** `POST /carvia/nfs/<int:nf_id>/reprocessar` (guard `sistema_carvia`) — toca routes.py = PROPOR.
  3. Cautelas: NAO reprocessar NF CANCELADA sem confirmacao; manter idempotencia no DELETE+reinsert;
     reprocessar dispara recalculo de peso cubado (R3) — checar fretes/operacoes ja vinculados antes
     de sobrescrever `valor_total`.
  Persistido v2 id 339 (HTTP 200).

## Sugestoes de skill F2 aguardando decisao Rafael+CC

> Prefixos `adhoc-`/`skill-gap-` (EXCECAO F2): NAO decididas/implementadas/persistidas — gate humano
> (`python scripts/agente/revisar_sugestoes_skill.py listar`). Apenas listadas.

| suggestion_key | Titulo | Origem | Membros | Idade | Nota |
|----------------|--------|--------|---------|-------|------|
| `adhoc-cluster-3734` | Configurar conta contabil de destino em operacoes CD/OUT no Odoo via XML-RPC | TAMIRIS SALLES CORDEIRO (id 44) | 6 | ~5d | **Reincidente** #49/#50/#51/#52. Gap REAL nao coberto (rel=`rastreando-odoo`, read-only). Candidata a atomo WRITE dry-run+confirmacao |
| `adhoc-cluster-1743` | Atualizar campos especificos de fretes CarVia (valor_cotado, peso_cubado, pedagio) por ID/criterio | TALITA DE LE LIMA (id 17) | 43 | ~4d | **Reincidente** #50/#51/#52. `valor_cotado`-por-id JA coberto por `atualizando_frete_carvia.py:203-218`; residual = (a) patch lote-dict (`--frete-id` so single) + (b) pedagio/peso-cubado subcontrato (script "NAO calcula" :14) |
| `skill-gap-razao-geral-odoo-script-necess-rio-para-c-lculos-customiz` | Razao Geral Odoo: saldo acumulado + debug de retorno + export Excel por conta/periodo | TAMIRIS SALLES CORDEIRO (id 44) | 4 | <1d | **NOVA**. rel=`razao-geral-odoo`. Pede saldo acumulado como saida + encapsular busca→Excel + documentar schema |
| `skill-gap-razao-geral-odoo-skill-razao-geral-odoo-exporta-relat-rio` | Razao Geral Odoo: conciliacao por conta com agrupamento por documento e tipo de diario | TAMIRIS SALLES CORDEIRO (id 44) | 5 | <1d | **NOVA**. rel=`razao-geral-odoo`. Mesmo cluster 5185; pede agrupamento por documento/diario + saldo acumulado |

## Resumo
- Total avaliadas: **2** (IMP- avaliaveis; 4 F2 apenas listadas; 0 reconciliadas no 1.5)
- Implementadas automaticamente: **1** (IMP-2026-06-29-001 — 3 bugs parser CarVia + teste, 23 passed)
- Rejeitadas: **0**
- Propostas para revisao humana: **1** (IMP-2026-06-29-002 — reprocessar NF CarVia)
- Persistencia: v2 ids 338, 339 — ambos HTTP 200
- Branch `cron/manutencao` (worktree), sem push (fluxo 4-maos)
