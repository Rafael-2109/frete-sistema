# Fluxo 2.6 — Tratar reserva ATIVA bloqueando transferência futura

**Quando:** antes de uma transferência (Skill 2 modo A/B/C), o saldo do quant origem está parcial ou totalmente RESERVADO por picking(s) ativo(s). Se prosseguir cego, a transferência falha (`UserError` Odoo "qty disponível < demanda") ou — pior — desencadeia comportamento imprevisto. Este fluxo INVESTIGA quem está reservando e oferece os **5 caminhos seguros** para liberar.

> **ATUALIZAcaO v8 (2026-05-25):** validado em PROD com caso FB/OUT/01046 (23 MLs onde 3 bloqueavam transferencia e 20 eram devolucoes validas). Caminho E (cirurgia) provou-se PREFERIVEL sobre caminho A (cancelar) quando picking tem MIX MLs validas + bloqueantes — preserva picking sem perder as 20 validas. Caminho D (outro lote alternativo) permanece o MAIS SEGURO quando estoque distribuido. Detalhes em VALIDACAO §14.

**Galho da árvore (subagente):** `2 Estoque (sem NF) → 2.6 tratar reserva bloqueia transferência (pré-Skill 2)`.

**Regra inviolável (gestor-estoque-odoo):** SEMPRE checar `reserved_quantity` via Skill 9 dos quants candidatos a DOAR ANTES de chamar Skill 2. SE `reserved > 0`, investigar pickings via fluxo 2.6 ANTES de prosseguir.

---

## Premissas (pesquisar+validar ANTES de qualquer write)

1. **Identificar quant candidato a doar** (origem da transferência):
   - Tipicamente o quant que a Skill 2 (modo A/B/C) vai reduzir.
   - Pode vir de planilha (cod + empresa + lote) ou de inspeção ad-hoc.
2. **Checar `reserved_quantity` real** via Skill 9 modo quants:
   ```bash
   python .claude/skills/consultando-quant-odoo/scripts/consultar_quants.py \
     --cods <cod> --empresas <emp> --com-lote <padrao>
   # Buscar campo "reserved" na saída
   ```
3. **Se reserved > 0** → seguir este fluxo. Caso contrário, ir direto para Skill 2.
4. **Identificar pickings reservando** via Skill 9 modo pickings (NOVO v7):
   ```bash
   python .claude/skills/consultando-quant-odoo/scripts/consultar_quants.py \
     --modo pickings --quant-ids <q1,q2,...>
   ```
   → retorna pickings DISTINCT com state, name, picking_type, origin, partner.
5. **Classificar cada picking encontrado**:
   - **state = `done`** → DEVOLVER (caminho B); preservação fiscal cuidadosa.
   - **state = `assigned/partially_available/confirmed/waiting`** → opções A/C/D/E:
     - **picking_type = OUT/Expedição** + **origin contendo "Devolução"** → caso fiscal complexo, **NUNCA cancelar sem consultar Fiscal**.
     - **picking_type = INT/Transferência Interna** sem origin + sem partner → provavelmente operação ad-hoc, candidato natural para CANCELAR (caminho A).
     - **picking_type = EMB/Separação Embalagem** + **origin = FB/OP/MANUAL/xxxxx** → MO ativa consumindo o lote; cancelar picking = furo em MO. Avaliar MO antes (Skill 4 `medir_consumo`).
     - **MLs órfãs** (`find_orphan_mls` retorna >0) → caminho E (cirurgia).
   - **OUTRO LOTE livre** com saldo suficiente → caminho D (mais seguro — não tocar reserva).

---

## 5 caminhos seguros para desreservar (tabela de decisão)

| Caminho | Comando | Quando usar | Risco | Reversível? |
|---|---|---|---|---|
| **A. Cancelar picking inteiro** | Skill 5 `--modo cancelar --picking-id <P>` OU Skill 2.4 `--cancelar-picking --picking-id <P>` | Picking SEM MLs válidas além das bloqueantes (fantasma; operação abandonada; INT sem origem/partner) | IRREVERSÍVEL. Tratamento fiscal se NF emitida (consultar Fiscal). **NÃO USAR se picking tem MLs válidas de OUTROS cods/devoluções** — caso v8 FB/OUT/01046 (23 MLs com 20 válidas). | ❌ |
| **B. Devolver picking** | Skill 5 `--modo devolver --picking-id <P>` | Picking state=`done` que precisa estornar saldo | Cria NF de devolução. Estorno fiscal pode ser necessário. | Parcial (via outra devolução) |
| **C. Desreservar mantendo picking** | Skill 2.4 `--unreserve-picking --picking-id <P>` (NOVO v7) | Operador quer liberar reserva mas manter picking para re-reservar com saldo atual | **RISCO conhecido (G_UNRESERVE_TRAVA)**: picking pode TRAVAR em assigned. **NÃO USAR em picking MIX** — libera TODAS as MLs (incluindo as válidas que precisam permanecer). | ✅ (re-reserva via Odoo) |
| **D. Não desreservar, usar OUTRO lote** | Skill 2 `--lote-origem <ALT>` ou `--lote <ALT> --para-indisponivel` | Existe outro lote livre com saldo suficiente para a doação | Mais seguro — não toca reserva existente. Mas pode não haver lote alternativo. **Validado v8: 11 cods MIGRAÇÃO resolvidos via D sem mexer em picking.** | ✅ |
| **E. Cirurgia ML bloqueante (preserva picking)** | Skill 2.4 `--ml-ids <ML1,ML2,...> --moves-writes "<M1>:0,<M2>:0,..."` + `--zerar-residual --quant-ids <Q1,Q2,...>` + Skill 2 MODO C transferindo lote livre | Picking tem MIX MLs válidas + bloqueantes (caso FB/OUT/01046 23 MLs); quer preservar MLs válidas. **PREFERIDO sobre A neste caso.** | Cirurgia segura — preserva picking. Deixa 3 moves residuais com qty=0 (cosmético; operador valida no Odoo UI = limpeza automática). Caminho validado v8. | Parcial (picking preservado, MLs específicas removidas; reservas liberadas) |

**Regra de seleção (refinada v8):**
- **D primeiro** (mais seguro, sem risco fiscal, sem tocar picking) — verificar alternativas via Skill 9 antes.
- **E quando picking tem MIX MLs válidas + bloqueantes** (validado v8 — FB/OUT/01046).
- **A só se picking é 100% bloqueante/fantasma** sem MLs válidas (caso v7 — FB/INT/08022).
- **B se picking já em done** com estorno necessário.
- **C como último recurso** (G_UNRESERVE_TRAVA + libera tudo do picking).

---

## Sequência (átomos componíveis; output de um alimenta input do próximo)

```
1) (HOST) Skill 9 modo quants — checar reserved_quantity dos quants candidatos
   python .claude/skills/consultando-quant-odoo/scripts/consultar_quants.py \
     --cods <cods> --empresas <emps> --com-lote <padrao> --formato json
   → para CADA quant, ler campo "reserved_quantity" no JSON.

2) Se TODOS reserved == 0 → SAIR deste fluxo, prosseguir com Skill 2 direto.

3) Para quants com reserved > 0 (FOR EACH quant):
   3.1) (HOST) Skill 9 modo pickings — identificar pickings reservando
        python .claude/skills/consultando-quant-odoo/scripts/consultar_quants.py \
          --modo pickings --quant-ids <Q1>
        → output: lista pickings DISTINCT com state, picking_type, origin, partner.

   3.2) (HOST) Para CADA picking:
        - Classificar conforme tabela (state + type + origin + partner).
        - Identificar caminho candidato (A/B/C/D/E).
        - Se houver MLs órfãs misturadas: rodar Skill 2.4 --find-orphan --quant-ids <Q>
          para confirmar (n MLs órfãs >0).

   3.3) (HOST) Apresentar ao USUÁRIO via AskUserQuestion:
        - "Quant X tem reserved Y reservado por picking Z (state=W, type=T,
          origin=O, partner=P). Caminho recomendado: [A/B/C/D/E]. Confirma?"
        - Opções: A=cancelar / B=devolver / C=unreserve / D=outro_lote / E=cirurgia
          / F=PULAR este quant.

   3.4) Executar caminho escolhido (--dry-run primeiro, depois --confirmar):

        Caminho A (cancelar picking):
        python .claude/skills/operando-picking-odoo/scripts/operar_picking.py \
          --modo cancelar --picking-id <P>
        # ou via Skill 2.4: --cancelar-picking --picking-id <P>

        Caminho B (devolver — state=done):
        python .claude/skills/operando-picking-odoo/scripts/operar_picking.py \
          --modo devolver --picking-id <P>

        Caminho C (unreserve mantendo picking — RISCO TRAVA):
        python .claude/skills/operando-reservas-odoo/scripts/operar_reserva.py \
          --unreserve-picking --picking-id <P>
        # output esperado: n_mls_depois=0 + state=confirmed/waiting
        # se output mostrar aviso "G_UNRESERVE_TRAVA" + state=assigned →
        # caminho falhou parcialmente; reconsiderar A.

        Caminho D (outro lote — NÃO toca reserva):
        # Saltar este quant; usar Skill 2 com --lote-origem alternativo.

        Caminho E (cirurgia ML bloqueante — preserva picking; PREFERIDO se picking tem MIX):
        # 1) Cirurgia: unlink MLs bloqueantes + zerar product_uom_qty dos moves parent
        python .claude/skills/operando-reservas-odoo/scripts/operar_reserva.py \
          --picking-id <P> --ml-ids <ML1,ML2,...> \
          --moves-writes "<M1>:0,<M2>:0,..." --confirmar
        # 2) Zerar reserved residual nos quants alvo (apos unlink, reserved fica negativo)
        python .claude/skills/operando-reservas-odoo/scripts/operar_reserva.py \
          --zerar-residual --quant-ids <Q1,Q2,...> --confirmar
        # 3) Transferir os quants livres (MODO C ou Skill 1 quant_id direto)
        python .claude/skills/transferindo-interno-odoo/scripts/transferir.py \
          --cod <COD> --empresa FB --qty <QTY> --para-indisponivel --lote <LOTE_ORIGEM> --confirmar
        # OBS: 3 moves residuais com qty=0 ficam pendurados no picking ate
        # operador validar via Odoo UI (limpeza automatica no button_validate).
        # Cosmetico — nao bloqueia operacao.

4) (HOST) Skill 9 modo pickings — re-checar pos-tratamento:
   python .claude/skills/consultando-quant-odoo/scripts/consultar_quants.py \
     --modo pickings --quant-ids <Q1>
   → esperado: 0 pickings em assigned/partial OU pickings com state=cancel/done.

5) (HOST) Skill 9 modo quants — verificar reserved_quantity == 0:
   python .claude/skills/consultando-quant-odoo/scripts/consultar_quants.py \
     --cods <cod> --empresas <emp> --com-lote <padrao>
   → confirmar reserved=0 ANTES de prosseguir.

6) Se reserved=0 confirmado → CHAMAR Skill 2 (modo A/B/C) normalmente.

7) (HOST) Verificar resultado direto no Odoo (regra inviolável 6 do CLAUDE.md).
```

---

## Gotchas do fluxo

- **G_UNRESERVE_TRAVA** (caminho C): `stock.picking.do_unreserve` retorna sem erro mas picking pode CONTINUAR em `state=assigned` se Odoo re-reservar automaticamente (depende de trigger). Sempre verificar `n_mls_depois==0` no output da Skill 2.4 `unreserve_picking`.
- **G030** (Skill 9): `stock.move.line.quant_id` é computed `store: False` — não filtrar por ele direto. A Skill 9 `listar_pickings_por_quant` resolve via tupla (product, lot, location, company) internamente.
- **Picking fiscal (OUT/Expedição com origin=Devolução)**: NUNCA cancelar (caminho A) sem consultar Fiscal — pode invalidar NF de devolução já emitida.
- **MO ativa consumindo lote** (EMB com origin=FB/OP/MANUAL): cancelar picking = furo em MO. Avaliar MO via Skill 4 `medir_consumo_mo` ANTES; se MO tem consumo>0, prefira caminho D (outro lote).
- **MLs órfãs misturadas**: caminho E exige `moves_writes` calculado por MOVE PARENT, não por ML (1 move pode ter 1 órfã + N OK; `product_uom_qty` novo = soma das MLs OK).
- **`reserved` negativo pos-cirurgia**: SEMPRE chamar `zerar_reserved_residual` após caminho E.
- **Caminho D é PREFERIDO** quando há lote alternativo livre — sem risco fiscal, sem mexer em reserva, atômico.

---

## Exemplo 2 — Caso real v8 2026-05-25 (cirurgia FB/OUT/01046 destravamento 876 un)

**Contexto:** Apos batch v8 ter resolvido 11 dos 13 cods MIGRAÇÃO residuais via caminho D (outros lotes alternativos), restavam 2 cods bloqueados: 103000117 (620.32 un) e 104000054 (255.79 un), reservados pelo picking FB/OUT/01046 (DEVOLUÇÃO de FB/IN/13196, partner LA FAMIGLIA - LF).

**Investigação AO VIVO revelou:** picking tinha **23 MLs** (não 3 como o filtro inicial mostrou) — 20 eram devoluções válidas de outros cods que NÃO podiam ser perdidas.

**Decisão:** caminho **E (cirurgia)** preferido sobre caminho A (cancelar) — A teria perdido as 20 MLs válidas.

**Execução (3 passos atomicos):**

```bash
SK24=.claude/skills/operando-reservas-odoo/scripts/operar_reserva.py
SK2=.claude/skills/transferindo-interno-odoo/scripts/transferir.py

# 1) Identificar moves parent das 3 MLs alvo (cada uma tem move 1:1)
# MLs: 217658245 (103000117 lot=MIGRACAO qty=620.32) → move 1161587
#      217658268 (103000113 lot=MIGRACAO qty=14.35)  → move 1161611
#      217658270 (104000054 lot=MIGRACAO qty=255.79) → move 1161613

# 2) Cirurgia (Skill 2.4): unlink 3 MLs + zerar product_uom_qty dos 3 moves
python "$SK24" --picking-id 320846 \
  --ml-ids 217658245,217658268,217658270 \
  --moves-writes "1161587:0,1161611:0,1161613:0" \
  --quiet --confirmar
# → CIRURGIA_OK em 1.24s — picking preservado com 20 MLs validas

# 3) Zerar reserved residual nos 3 quants alvo (apos unlink, reserved fica negativo)
python "$SK24" --zerar-residual \
  --quant-ids 258942,258944,258987 \
  --quiet --confirmar
# → ZERAR_RESIDUAL_OK em 0.75s — 3 quants com reserved=0

# 4) Transferir os 3 lotes livres via Skill 2 MODO C
for caso in "103000117 620.32" "103000113 14.3514" "104000054 255.7932"; do
  c=$(echo $caso | cut -d' ' -f1); q=$(echo $caso | cut -d' ' -f2)
  python "$SK2" --cod "$c" --empresa FB --qty "$q" \
    --para-indisponivel --lote MIGRACAO --quiet --confirmar
done
# → 3/3 EXECUTADO (890.46 un para MIGRAÇÃO FB/Indisp)
```

**Resultado:** 890 un destravadas + picking preservado (20 MLs intactas). 3 moves residuais com qty=0 ficam no picking ate operador validar via Odoo UI (cosmético — `button_validate` limpa automaticamente).

**Estado final:** 67/67 cods executaveis do plano 71 cods OK (100%) + 4 SKIP planejados.

---

## Exemplo 1 — Caso real 2026-05-24 (caminho A — FB/INT/08022)

**Contexto**: Etapa A (Skill 2 modo A: lote → MIGRAÇÃO em FB/Estoque) descobriu que 5+3=8 cods têm reserved>0:
- **Lote 13206** reservado em 3 cods (4899027, 4890128, 4902852): 1 picking `FB/INT/08022` (Transferência Interna FB, sem origin/partner) reserva 1035.083 un.
- **MIGRAÇÃO em FB/Estoque** reservado em 5 cods: 3 pickings:
  - `FB/FB/EMB/11673` (Separação Embalagem, origin=`FB/OP/MANUAL/01763`, 0.006 de 105000038): consumo MO ativa.
  - `FB/FB/EMB/11674` (origin=`FB/OP/MANUAL/01764`, 0.30 de 105000038+105000021): consumo MO ativa.
  - `FB/OUT/01046` (Expedição, origin=`Devolução de FB/IN/13196`, partner=`LA FAMIGLIA - LF`, 890.46 de 103000117+103000113+104000054): DEVOLUÇÃO real para LF.

**Classificação esperada (caso retomado pos-fluxo 2.6)**:
- **3 cods lote 13206** (1 picking FB/INT/08022): caminho **A** (cancelar — picking INT sem origem, sem valor fiscal aparente). MAS antes confirmar com Rafael — pode ser operação em curso.
- **2 cods de MO ativa** (105000038, 105000021): caminho **D** se houver outro lote livre, OU caminho **C** (unreserve) seguido de re-MO; OU **PULAR** estes 2 cods.
- **3 cods de DEVOLUÇÃO** (103000117, 103000113, 104000054): caminho **D** se houver outro lote livre; se não, **PULAR** (não cancelar/devolver picking fiscal sem consultar Fiscal).

**Sequência de invocação**:
```bash
SK9=.claude/skills/consultando-quant-odoo/scripts/consultar_quants.py
SK24=.claude/skills/operando-reservas-odoo/scripts/operar_reserva.py
SK5=.claude/skills/operando-picking-odoo/scripts/operar_picking.py
SK2=.claude/skills/transferindo-interno-odoo/scripts/transferir.py

# 1) Auditoria reserva (já feita em sessão anterior — 8 cods com reserved>0)
python "$SK9" --cods 4899027,4890128,4902852,103000113,104000054,105000021,105000038,103000117 --empresas FB --formato json | jq '.quants[] | {cod, reserved_quantity}'

# 2) Identificar pickings reservando lote 13206
python "$SK9" --modo pickings --quant-ids 261590,261594,261598

# 3) AskUserQuestion (HOST): "Cancelar FB/INT/08022 (Transferência Interna FB)?"
# Resposta: A (cancelar)

# 4) Dry-run + confirmar
python "$SK5" --modo cancelar --picking-id 320753       # dry-run
python "$SK5" --modo cancelar --picking-id 320753 --confirmar

# 5) Re-checar
python "$SK9" --modo pickings --quant-ids 261590,261594,261598  # 0 pickings

# 6) Prosseguir Skill 2 (Etapa A original)
python "$SK2" --modo A --cod 4899027 --lote-origem 13206 --qty 269 --confirmar
# ... (repetir para 4890128 e 4902852)
```

---

## Scripts-fonte cobertos (não há scripts ad-hoc — fluxo NOVO)

Este fluxo NASCE da demanda real 2026-05-24 (caso 71-cods PAUSADO por gap arquitetural). Documentado em `docs/inventario-2026-05/casos-pendentes/CASO_PENDENTE_RESERVAS_71_CODS_2026_05_24.md`. Após primeira execução completa do fluxo, atualizar com lições aprendidas.

| Script | Pattern minerado | Substituído por |
|---|---|---|
| (nenhum) | Fluxo NOVO — nasceu de demanda real | Composição de skills 9, 2.4, 5, 2 |

---

## Pré-requisitos técnicos

- **Skill 9** modo pickings (NOVO v7) — implementado em `app/odoo/estoque/scripts/consulta_quant.py:434+`.
- **Skill 2.4** átomos `unreserve_picking` + `find_orphan_mls` (NOVOS v7) — implementados em `app/odoo/estoque/scripts/reserva.py:225+`.
- **Skill 5** átomos `cancelar` + `devolver` (já implementados — sessão v3).
- **Skill 2** modo A/B/C (já implementados — sessão v2/v4).

---

## Relacionado

- [Fluxo 2.4 — cancelar reserva órfã](2.4-cancelar-reserva-orfa.md) — irmão (pos-mortem; este fluxo é pré-mortem).
- [Fluxo 2.2 — realocar saldo](2.2-realocar-saldo.md) — chamado APÓS este fluxo libera saldo.
- Gotcha [G030](../../../docs/inventario-2026-05/02-gotchas/G030-quant-id-em-stock-move-line-eh-computed.md) — cross-ref ML→quant via tupla.
- Memória de origem (`caso_real_tratar_reservas_pre_transferencia`) aposentada 2026-06-11 — padrão integralmente codificado neste fluxo.
