{
  "mappings": {
    "description": "Mapping between current Claude AI Novo endpoints and MCP tools",
    "endpoints": [
      {
        "current": {
          "path": "/claude-ai/api/query",
          "method": "POST",
          "handler": "WebIntegration.process_query"
        },
        "mcp": {
          "workflow": [
            {
              "tool": "frete_query_analyzer",
              "description": "Analyze the incoming query"
            },
            {
              "tool": "frete_data_loader",
              "description": "Load relevant data based on analysis"
            },
            {
              "tool": "frete_context_manager",
              "description": "Manage session context"
            },
            {
              "tool": "frete_response_generator",
              "description": "Generate optimized response"
            }
          ]
        }
      },
      {
        "current": {
          "path": "/claude-ai/chat",
          "method": "GET/POST",
          "handler": "render chat interface"
        },
        "mcp": {
          "integration": "Claude Desktop native interface",
          "notes": "Users will interact directly through Claude Desktop app"
        }
      },
      {
        "current": {
          "path": "/health",
          "method": "GET",
          "handler": "health_check"
        },
        "mcp": {
          "resource": "frete://status/system",
          "notes": "Real-time system status through MCP resource"
        }
      }
    ],
    "dataFlow": {
      "current": "User → Flask → WebIntegration → Orchestrators → Processors → Response",
      "mcp": "User → Claude Desktop → MCP Server → Tools → Response"
    },
    "advantages": {
      "performance": "Direct execution in Claude Desktop eliminates network overhead",
      "context": "Native context management with persistent memory",
      "tools": "Modular tools can be combined flexibly",
      "maintenance": "Type-safe TypeScript with better error handling"
    }
  }
}